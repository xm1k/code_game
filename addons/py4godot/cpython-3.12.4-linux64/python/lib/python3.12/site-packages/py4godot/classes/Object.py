# distutils: language=c++
import py4godot.utils.functools as  functools
import py4godot.utils.utils as utils
from py4godot.classes.core import *
from py4godot.utils.smart_cast import smart_cast, register_cast_function, register_class, get_class
from py4godot.utils.CPPWrapper import CPPWrapper, constructor, static_method
import py4godot.utils.utils as c_utils
from py4godot.utils.CoreType import CoreType
import typing
import py4godot.variant_types
import py4godot.signals as signals

import typing
if typing.TYPE_CHECKING:
  pass
  import py4godot.classes.StringNameTypedArray as py4godot_stringnametypedarray 
  import py4godot.classes.DictionaryTypedArray as py4godot_dictionarytypedarray 
import py4godot.classes.StringNameTypedArray as py4godot_stringnametypedarray 
import py4godot.classes.DictionaryTypedArray as py4godot_dictionarytypedarray 
from py4godot.wrappers.wrappers import CPPObjectWrapper

class ConnectFlags:
  CONNECT_DEFERRED = 1
  CONNECT_PERSIST = 2
  CONNECT_ONE_SHOT = 4
  CONNECT_REFERENCE_COUNTED = 8
  CONNECT_APPEND_SOURCE_OBJECT = 16


class Object():
  r'''
		An advanced `Variant` type. All classes in the engine inherit from Object. Each class may define new properties, methods or signals, which are available to all inheriting classes. For example, a `Sprite2D` instance is able to call `Node.add_child` because it inherits from `Node`.
		You can create new instances, using `Object.new()` in GDScript, or `new GodotObject` in C#.
		To delete an Object instance, call `free`. This is necessary for most classes inheriting Object, because they do not manage memory on their own, and will otherwise cause memory leaks when no longer in use. There are a few classes that perform memory management. For example, `RefCounted` (and by extension `Resource`) deletes itself when no longer referenced, and `Node` deletes its children when freed.
		Objects can have a `Script` attached to them. Once the `Script` is instantiated, it effectively acts as an extension to the base class, allowing it to define and inherit new properties, methods and signals.
		Inside a `Script`, `_get_property_list` may be overridden to customize properties in several ways. This allows them to be available to the editor, display as lists of options, sub-divide into groups, save on disk, etc. Scripting languages offer easier ways to customize properties, such as with the `annotation @GDScript.@export` annotation.
		Godot is very dynamic. An object's script, and therefore its properties, methods and signals, can be changed at run-time. Because of this, there can be occasions where, for example, a property required by a method may not exist. To prevent run-time errors, see methods such as `set`, `get`, `call`, `has_method`, `has_signal`, etc. Note that these methods are **much** slower than direct references.
		In GDScript, you can also check if a given property, method, or signal name exists in an object with the `in` operator:
		```gdscript
		var node = Node.new()
		print("name" in node)         # Prints true
		print("get_parent" in node)   # Prints true
		print("tree_entered" in node) # Prints true
		print("unknown" in node)      # Prints false
		```
		Notifications are `int` constants commonly sent and received by objects. For example, on every rendered frame, the `SceneTree` notifies nodes inside the tree with a `constant Node.NOTIFICATION_PROCESS`. The nodes receive it and may call `Node._process` to update. To make use of notifications, see `notification` and `_notification`.
		Lastly, every object can also contain metadata (data about data). `set_meta` can be useful to store information that the object itself does not depend on. To keep your code clean, making excessive use of metadata is discouraged.
		**Note:** Unlike references to a `RefCounted`, references to an object stored in a variable can become invalid without being set to `null`. To check if an object has been deleted, do _not_ compare it against `null`. Instead, use `@GlobalScope.is_instance_valid`. It's also recommended to inherit from `RefCounted` for classes storing data instead of `Object`.
		**Note:** The `script` is not exposed like most properties. To set or get an object's `Script` in code, use `set_script` and `get_script`, respectively.
		**Note:** In a boolean context, an `Object` will evaluate to `false` if it is equal to `null` or it has been freed. Otherwise, an `Object` will always evaluate to `true`. See also `@GlobalScope.is_instance_valid`.
	'''
  NOTIFICATION_POSTINITIALIZE:typing.ClassVar[int]

  NOTIFICATION_PREDELETE:typing.ClassVar[int]

  NOTIFICATION_EXTENSION_RELOADED:typing.ClassVar[int]

  @staticmethod
  def constructor():
    class_ = Object.construct_without_init()
    class_._ptr = constructor(523,0, ())
    return class_
  @staticmethod
  def new():
    class_ = Object.construct_without_init()
    class_._ptr = constructor(523,0, ())
    return class_
  def __init__(self):
    self._ptr =  CPPObjectWrapper()
    self.casted_from = None
    if c_utils.shouldCreateObject:
      self._ptr = constructor(523,0, ())


  def get_pyscript(self):
    id = self.get_instance_id()
    script = c_utils.py_get_py_script(id)
    return script

  @property
  def _import_path(self):
    _ret = String.new0()
    #_ret._ptr = self.Object_internal_class_ptr.get().get_import_path()
    return c_utils.gd_string_to_py_string(_ret)
  @_import_path.setter
  def _import_path(self, val):
    pass

  def destroy(self):
    self.Object_internal_class_ptr.get().destroy()
  def generate_wrapper(self):
    return CPPObjectWrapper()
  def init_signals(self):
    script_changed_name = utils.py_string_to_string_name("script_changed")
    self.script_changed = signals.BuiltinSignal(self, script_changed_name)
    property_list_changed_name = utils.py_string_to_string_name("property_list_changed")
    self.property_list_changed = signals.BuiltinSignal(self, property_list_changed_name)

  @staticmethod
  def construct_without_init():
    cls = Object.__new__(Object)
    cls.shouldBeDeleted = True
    cls.casted_from = None
    cls.init_signals()
    return cls
  @staticmethod
  def get_type():
    return 'Object'

  def __del__(self):
    pass
  @staticmethod
  def cast(other:'Object'):
    assert other != None # Object to be casted must not be None
    cls = Object.construct_without_init()
    cls._ptr = CPPObjectWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls
  @staticmethod
  def cast_without_reference(other: 'Object'):
    assert other != None # Object to be casted must not be None
    cls = Object.construct_without_init()
    cls._ptr = CPPObjectWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls

  @functools.native_method
  def get_class(self) -> typing.Union[str]:
    r'''
				Returns the object's built-in class name, as a `String`. See also `is_class`.
				**Note:** This method ignores `class_name` declarations. If this object's script has defined a `class_name`, the base, built-in class name is returned instead.
			'''




    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(0,tuple([]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def is_class(self, class_:'str'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the object inherits from the given `class`. See also `get_class`.
				
				```gdscript
				var sprite2d = Sprite2D.new()
				sprite2d.is_class("Sprite2D") # Returns true
				sprite2d.is_class("Node")     # Returns true
				sprite2d.is_class("Node3D")   # Returns false
				```
				
				
				**Note:** This method ignores `class_name` declarations in the object's script.
			'''

    assert(not class_ is None)
    assert isinstance(class_, (str, String)), 'class_ must be str or String'






    assert(isinstance(class_, (str, String)))
    py_string_class_ = class_ if isinstance(class_, StringName) else c_utils.py_string_to_string(class_)

    _ret = 0
    _ret = self._ptr.call_with_return(1,tuple([py_string_class_._ptr]))
    return _ret


  @functools.native_method
  def set(self, property:'object'   , value:'object'   ) -> None:
    r'''
				Assigns `value` to the given `property`. If the property does not exist or the given `value`'s type doesn't match, nothing happens.
				
				```gdscript
				var node = Node2D.new()
				node.set("global_scale", Vector2(8, 2.5))
				print(node.global_scale) # Prints (8.0, 2.5)
				```
				
				
				**Note:** In C#, `property` must be in snake_case when referring to built-in Godot properties. Prefer using the names exposed in the `PropertyName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not property is None)
    assert isinstance(property, (str, StringName)), 'property must be str or StringName'
    

    if not (type(value).__name__ in py4godot.variant_types.core_types or isinstance(value, Object)):
      raise ValueError(
                f"Unsupported type for argument 'value': "
                f"{type(value).__name__}. "
                "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
                "and built-in types int, float, and bool."
            )





    assert(isinstance(property, (str, StringName)))
    py_stringname_property = property if isinstance(property, StringName) else c_utils.py_string_to_string_name(property)


    self._ptr.call_with_return(2,tuple([py_stringname_property._ptr, value]))

  @functools.native_method
  def get(self, property:'object'   ) -> typing.Union[typing.Any]:
    r'''
				Returns the `Variant` value of the given `property`. If the `property` does not exist, this method returns `null`.
				
				```gdscript
				var node = Node2D.new()
				node.rotation = 1.5
				var a = node.get("rotation") # a is 1.5
				```
				
				
				**Note:** In C#, `property` must be in snake_case when referring to built-in Godot properties. Prefer using the names exposed in the `PropertyName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not property is None)
    assert isinstance(property, (str, StringName)), 'property must be str or StringName'






    assert(isinstance(property, (str, StringName)))
    py_stringname_property = property if isinstance(property, StringName) else c_utils.py_string_to_string_name(property)

    _ret = None
    _ret = self._ptr.call_with_return(3,tuple([py_stringname_property._ptr]))
    return _ret


  @functools.native_method
  def set_indexed(self, property_path:'object'   , value:'object'   ) -> None:
    r'''
				Assigns a new `value` to the property identified by the `property_path`. The path should be a `NodePath` relative to this object, and can use the colon character (`:`) to access nested properties.
				
				```gdscript
				var node = Node2D.new()
				node.set_indexed("position", Vector2(42, 0))
				node.set_indexed("position:y", -10)
				print(node.position) # Prints (42.0, -10.0)
				```
				
				
				**Note:** In C#, `property_path` must be in snake_case when referring to built-in Godot properties. Prefer using the names exposed in the `PropertyName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not property_path is None)
    assert isinstance(property_path, (str, NodePath)), 'property_path must be str or NodePath'
    

    if not (type(value).__name__ in py4godot.variant_types.core_types or isinstance(value, Object)):
      raise ValueError(
                f"Unsupported type for argument 'value': "
                f"{type(value).__name__}. "
                "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
                "and built-in types int, float, and bool."
            )





    assert(isinstance(property_path, (str, NodePath)))
    py_nodepath_property_path = property_path if isinstance(property_path,NodePath) else NodePath.new2(property_path)


    self._ptr.call_with_return(4,tuple([py_nodepath_property_path._ptr, value]))

  @functools.native_method
  def get_indexed(self, property_path:'object'   ) -> typing.Union[typing.Any]:
    r'''
				Gets the object's property indexed by the given `property_path`. The path should be a `NodePath` relative to the current object and can use the colon character (`:`) to access nested properties.
				**Examples:** `"position:x"` or `"material:next_pass:blend_mode"`.
				
				```gdscript
				var node = Node2D.new()
				node.position = Vector2(5, -10)
				var a = node.get_indexed("position")   # a is Vector2(5, -10)
				var b = node.get_indexed("position:y") # b is -10
				```
				
				
				**Note:** In C#, `property_path` must be in snake_case when referring to built-in Godot properties. Prefer using the names exposed in the `PropertyName` class to avoid allocating a new `StringName` on each call.
				**Note:** This method does not support actual paths to nodes in the `SceneTree`, only sub-property paths. In the context of nodes, use `Node.get_node_and_resource` instead.
			'''

    assert(not property_path is None)
    assert isinstance(property_path, (str, NodePath)), 'property_path must be str or NodePath'






    assert(isinstance(property_path, (str, NodePath)))
    py_nodepath_property_path = property_path if isinstance(property_path,NodePath) else NodePath.new2(property_path)

    _ret = None
    _ret = self._ptr.call_with_return(5,tuple([py_nodepath_property_path._ptr]))
    return _ret


  @functools.native_method
  def get_property_list(self) -> typing.Union['py4godot_dictionarytypedarray.DictionaryTypedArray']:
    r'''
				Returns the object's property list as an `Array` of dictionaries. Each `Dictionary` contains the following entries:
				- `name` is the property's name, as a `String`;
				- `class_name` is an empty `StringName`, unless the property is `constant TYPE_OBJECT` and it inherits from a class;
				- `type` is the property's type, as an `int` (see `enum Variant.Type`);
				- `hint` is _how_ the property is meant to be edited (see `enum PropertyHint`);
				- `hint_string` depends on the hint (see `enum PropertyHint`);
				- `usage` is a combination of `enum PropertyUsageFlags`.
				**Note:** In GDScript, all class members are treated as properties. In C# and GDExtension, it may be necessary to explicitly mark class members as Godot properties using decorators or attributes.
			'''




    _ret = py4godot_dictionarytypedarray.DictionaryTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(6,tuple([]))
    return _ret


  @functools.native_method
  def get_method_list(self) -> typing.Union['py4godot_dictionarytypedarray.DictionaryTypedArray']:
    r'''
				Returns this object's methods and their signatures as an `Array` of dictionaries. Each `Dictionary` contains the following entries:
				- `name` is the name of the method, as a `String`;
				- `args` is an `Array` of dictionaries representing the arguments;
				- `default_args` is the default arguments as an `Array` of variants;
				- `flags` is a combination of `enum MethodFlags`;
				- `id` is the method's internal identifier `int`;
				- `return` is the returned value, as a `Dictionary`;
				**Note:** The dictionaries of `args` and `return` are formatted identically to the results of `get_property_list`, although not all entries are used.
			'''




    _ret = py4godot_dictionarytypedarray.DictionaryTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(7,tuple([]))
    return _ret


  @functools.native_method
  def property_can_revert(self, property:'object'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the given `property` has a custom default value. Use `property_get_revert` to get the `property`'s default value.
				**Note:** This method is used by the Inspector dock to display a revert icon. The object must implement `_property_can_revert` to customize the default value. If `_property_can_revert` is not implemented, this method returns `false`.
			'''

    assert(not property is None)
    assert isinstance(property, (str, StringName)), 'property must be str or StringName'






    assert(isinstance(property, (str, StringName)))
    py_stringname_property = property if isinstance(property, StringName) else c_utils.py_string_to_string_name(property)

    _ret = 0
    _ret = self._ptr.call_with_return(8,tuple([py_stringname_property._ptr]))
    return _ret


  @functools.native_method
  def property_get_revert(self, property:'object'   ) -> typing.Union[typing.Any]:
    r'''
				Returns the custom default value of the given `property`. Use `property_can_revert` to check if the `property` has a custom default value.
				**Note:** This method is used by the Inspector dock to display a revert icon. The object must implement `_property_get_revert` to customize the default value. If `_property_get_revert` is not implemented, this method returns `null`.
			'''

    assert(not property is None)
    assert isinstance(property, (str, StringName)), 'property must be str or StringName'






    assert(isinstance(property, (str, StringName)))
    py_stringname_property = property if isinstance(property, StringName) else c_utils.py_string_to_string_name(property)

    _ret = None
    _ret = self._ptr.call_with_return(9,tuple([py_stringname_property._ptr]))
    return _ret


  @functools.native_method
  def notification(self, what:'int'   , reversed:'bool' =False  ) -> None:
    r'''
				Sends the given `what` notification to all classes inherited by the object, triggering calls to `_notification`, starting from the highest ancestor (the `Object` class) and going down to the object's script.
				If `reversed` is `true`, the call order is reversed.
				
				```gdscript
				var player = Node2D.new()
				player.set_script(load("res://player.gd"))

				player.notification(NOTIFICATION_ENTER_TREE)
				# The call order is Object -> Node -> Node2D -> player.gd.

				player.notification(NOTIFICATION_ENTER_TREE, true)
				# The call order is player.gd -> Node2D -> Node -> Object.
				```
				
				
			'''

    assert isinstance(what, (int, float)), 'what must be int or float'
    assert isinstance(reversed, bool), 'reversed must be bool'









    self._ptr.call_with_return(10,tuple([what, reversed]))

  @functools.native_method
  def to_string(self) -> typing.Union[str]:
    r'''
				Returns a `String` representing the object. Defaults to `"<ClassName#RID>"`. Override `_to_string` to customize the string representation of the object.
			'''




    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(11,tuple([]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_instance_id(self) -> typing.Union[int]:
    r'''
				Returns the object's unique instance ID. This ID can be saved in `EncodedObjectAsID`, and can be used to retrieve this object instance with `@GlobalScope.instance_from_id`.
				**Note:** This ID is only useful during the current session. It won't correspond to a similar object if the ID is sent over a network, or loaded from a file at a later time.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(12,tuple([]))
    return _ret


  @functools.native_method
  def set_script(self, script:'object'   ) -> None:
    r'''
				Attaches `script` to the object, and instantiates it. As a result, the script's `_init` is called. A `Script` is used to extend the object's functionality.
				If a script already exists, its instance is detached, and its property values and state are lost. Built-in property values are still kept.
			'''

    

    if not (type(script).__name__ in py4godot.variant_types.core_types or isinstance(script, Object)):
      raise ValueError(
                f"Unsupported type for argument 'script': "
                f"{type(script).__name__}. "
                "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
                "and built-in types int, float, and bool."
            )







    self._ptr.call_with_return(13,tuple([script]))

  @functools.native_method
  def get_script(self) -> typing.Union[typing.Any]:
    r'''
				Returns the object's `Script` instance, or `null` if no script is attached.
			'''




    _ret = None
    _ret = self._ptr.call_with_return(14,tuple([]))
    return _ret


  @functools.native_method
  def set_meta(self, name:'object'   , value:'object'   ) -> None:
    r'''
				Adds or changes the entry `name` inside the object's metadata. The metadata `value` can be any `Variant`, although some types cannot be serialized correctly.
				If `value` is `null`, the entry is removed. This is the equivalent of using `remove_meta`. See also `has_meta` and `get_meta`.
				**Note:** A metadata's name must be a valid identifier as per `StringName.is_valid_identifier` method.
				**Note:** Metadata that has a name starting with an underscore (`_`) is considered editor-only. Editor-only metadata is not displayed in the Inspector and should not be edited, although it can still be found by this method.
			'''

    assert(not name is None)
    assert isinstance(name, (str, StringName)), 'name must be str or StringName'
    

    if not (type(value).__name__ in py4godot.variant_types.core_types or isinstance(value, Object)):
      raise ValueError(
                f"Unsupported type for argument 'value': "
                f"{type(value).__name__}. "
                "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
                "and built-in types int, float, and bool."
            )





    assert(isinstance(name, (str, StringName)))
    py_stringname_name = name if isinstance(name, StringName) else c_utils.py_string_to_string_name(name)


    self._ptr.call_with_return(15,tuple([py_stringname_name._ptr, value]))

  @functools.native_method
  def remove_meta(self, name:'object'   ) -> None:
    r'''
				Removes the given entry `name` from the object's metadata. See also `has_meta`, `get_meta` and `set_meta`.
				**Note:** A metadata's name must be a valid identifier as per `StringName.is_valid_identifier` method.
				**Note:** Metadata that has a name starting with an underscore (`_`) is considered editor-only. Editor-only metadata is not displayed in the Inspector and should not be edited, although it can still be found by this method.
			'''

    assert(not name is None)
    assert isinstance(name, (str, StringName)), 'name must be str or StringName'






    assert(isinstance(name, (str, StringName)))
    py_stringname_name = name if isinstance(name, StringName) else c_utils.py_string_to_string_name(name)

    self._ptr.call_with_return(16,tuple([py_stringname_name._ptr]))

  @functools.native_method
  def get_meta(self, name:'object'   , default:'object' =None  ) -> typing.Union[typing.Any]:
    r'''
				Returns the object's metadata value for the given entry `name`. If the entry does not exist, returns `default`. If `default` is `null`, an error is also generated.
				**Note:** A metadata's name must be a valid identifier as per `StringName.is_valid_identifier` method.
				**Note:** Metadata that has a name starting with an underscore (`_`) is considered editor-only. Editor-only metadata is not displayed in the Inspector and should not be edited, although it can still be found by this method.
			'''


    assert(not name is None)
    assert isinstance(name, (str, StringName)), 'name must be str or StringName'
    

    if not (type(default).__name__ in py4godot.variant_types.core_types or isinstance(default, Object)):
      raise ValueError(
                f"Unsupported type for argument 'default': "
                f"{type(default).__name__}. "
                "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
                "and built-in types int, float, and bool."
            )





    assert(isinstance(name, (str, StringName)))
    py_stringname_name = name if isinstance(name, StringName) else c_utils.py_string_to_string_name(name)


    _ret = None
    _ret = self._ptr.call_with_return(17,tuple([py_stringname_name._ptr, default]))
    return _ret


  @functools.native_method
  def has_meta(self, name:'object'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if a metadata entry is found with the given `name`. See also `get_meta`, `set_meta` and `remove_meta`.
				**Note:** A metadata's name must be a valid identifier as per `StringName.is_valid_identifier` method.
				**Note:** Metadata that has a name starting with an underscore (`_`) is considered editor-only. Editor-only metadata is not displayed in the Inspector and should not be edited, although it can still be found by this method.
			'''

    assert(not name is None)
    assert isinstance(name, (str, StringName)), 'name must be str or StringName'






    assert(isinstance(name, (str, StringName)))
    py_stringname_name = name if isinstance(name, StringName) else c_utils.py_string_to_string_name(name)

    _ret = 0
    _ret = self._ptr.call_with_return(18,tuple([py_stringname_name._ptr]))
    return _ret


  @functools.native_method
  def get_meta_list(self) -> typing.Union['py4godot_stringnametypedarray.StringNameTypedArray']:
    r'''
				Returns the object's metadata entry names as an `Array` of `StringName`s.
			'''




    _ret = py4godot_stringnametypedarray.StringNameTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(19,tuple([]))
    return _ret


  @functools.native_method
  def add_user_signal(self, signal:'str'   , arguments:'Array' = None  ) -> None:
    r'''
				Adds a user-defined signal named `signal`. Optional arguments for the signal can be added as an `Array` of dictionaries, each defining a `name` `String` and a `type` `int` (see `enum Variant.Type`). See also `has_user_signal` and `remove_user_signal`.
				
				```gdscript
				add_user_signal("hurt", `
					{ "name": "damage", "type": TYPE_INT },
					{ "name": "source", "type": TYPE_OBJECT }
				`)
				```
				
				
			'''
    if arguments is None:
      arguments = Array.new0()

    assert(not signal is None)
    assert isinstance(signal, (str, String)), 'signal must be str or String'






    assert(isinstance(signal, (str, String)))
    py_string_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string(signal)


    self._ptr.call_with_return(20,tuple([py_string_signal._ptr, arguments._ptr]))

  @functools.native_method
  def has_user_signal(self, signal:'object'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the given user-defined `signal` name exists. Only signals added with `add_user_signal` are included. See also `remove_user_signal`.
			'''

    assert(not signal is None)
    assert isinstance(signal, (str, StringName)), 'signal must be str or StringName'






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)

    _ret = 0
    _ret = self._ptr.call_with_return(21,tuple([py_stringname_signal._ptr]))
    return _ret


  @functools.native_method
  def remove_user_signal(self, signal:'object'   ) -> None:
    r'''
				Removes the given user signal `signal` from the object. See also `add_user_signal` and `has_user_signal`.
			'''

    assert(not signal is None)
    assert isinstance(signal, (str, StringName)), 'signal must be str or StringName'






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)

    self._ptr.call_with_return(22,tuple([py_stringname_signal._ptr]))

  @functools.native_method
  def emit_signal(self, signal:'object'   , *varargs) -> typing.Union[int]:
    r'''
				Emits the given `signal` by name. The signal must exist, so it should be a built-in signal of this class or one of its inherited classes, or a user-defined signal (see `add_user_signal`). This method supports a variable number of arguments, so parameters can be passed as a comma separated list.
				Returns `constant ERR_UNAVAILABLE` if `signal` does not exist or the parameters are invalid.
				
				```gdscript
				emit_signal("hit", "sword", 100)
				emit_signal("game_over")
				```
				
				
				**Note:** In C#, `signal` must be in snake_case when referring to built-in Godot signals. Prefer using the names exposed in the `SignalName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not signal is None)
    assert isinstance(signal, (str, StringName)), 'signal must be str or StringName'

    for arg in varargs:
      if not (type(arg).__name__ in py4godot.variant_types.core_types or isinstance(arg, Object)):
        raise ValueError(
            f"Unsupported type in varargs: {type(arg).__name__}. "
            "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
            "and built-in types int, float, and bool."
        )




    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)

    _ret:int
    _ret = self._ptr.call_with_return(23,tuple([py_stringname_signal._ptr, *varargs]))
    return _ret


  @functools.native_method
  def call(self, method:'object'   , *varargs) -> typing.Union[typing.Any]:
    r'''
				Calls the `method` on the object and returns the result. This method supports a variable number of arguments, so parameters can be passed as a comma separated list.
				
				```gdscript
				var node = Node3D.new()
				node.call("rotate", Vector3(1.0, 0.0, 0.0), 1.571)
				```
				
				
				**Note:** In C#, `method` must be in snake_case when referring to built-in Godot methods. Prefer using the names exposed in the `MethodName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not method is None)
    assert isinstance(method, (str, StringName)), 'method must be str or StringName'

    for arg in varargs:
      if not (type(arg).__name__ in py4godot.variant_types.core_types or isinstance(arg, Object)):
        raise ValueError(
            f"Unsupported type in varargs: {type(arg).__name__}. "
            "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
            "and built-in types int, float, and bool."
        )




    assert(isinstance(method, (str, StringName)))
    py_stringname_method = method if isinstance(method, StringName) else c_utils.py_string_to_string_name(method)

    _ret = None
    _ret = self._ptr.call_with_return(24,tuple([py_stringname_method._ptr, *varargs]))
    return _ret


  @functools.native_method
  def call_deferred(self, method:'object'   , *varargs) -> typing.Union[typing.Any]:
    r'''
				Calls the `method` on the object during idle time. Always returns `null`, **not** the method's result.
				Idle time happens mainly at the end of process and physics frames. In it, deferred calls will be run until there are none left, which means you can defer calls from other deferred calls and they'll still be run in the current idle time cycle. This means you should not call a method deferred from itself (or from a method called by it), as this causes infinite recursion the same way as if you had called the method directly.
				This method supports a variable number of arguments, so parameters can be passed as a comma separated list.
				
				```gdscript
				var node = Node3D.new()
				node.call_deferred("rotate", Vector3(1.0, 0.0, 0.0), 1.571)
				```
				
				
				See also `Callable.call_deferred`.
				**Note:** In C#, `method` must be in snake_case when referring to built-in Godot methods. Prefer using the names exposed in the `MethodName` class to avoid allocating a new `StringName` on each call.
				**Note:** If you're looking to delay the function call by a frame, refer to the `signal SceneTree.process_frame` and `signal SceneTree.physics_frame` signals.
				```gdscript
				var node = Node3D.new()
				# Make a Callable and bind the arguments to the node's rotate() call.
				var callable = node.rotate.bind(Vector3(1.0, 0.0, 0.0), 1.571)
				# Connect the callable to the process_frame signal, so it gets called in the next process frame.
				# CONNECT_ONE_SHOT makes sure it only gets called once instead of every frame.
				get_tree().process_frame.connect(callable, CONNECT_ONE_SHOT)
				```
			'''

    assert(not method is None)
    assert isinstance(method, (str, StringName)), 'method must be str or StringName'

    for arg in varargs:
      if not (type(arg).__name__ in py4godot.variant_types.core_types or isinstance(arg, Object)):
        raise ValueError(
            f"Unsupported type in varargs: {type(arg).__name__}. "
            "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
            "and built-in types int, float, and bool."
        )

    for arg in varargs:
      if isinstance(arg, CoreType):
        arg.shouldBeDeleted=False




    assert(isinstance(method, (str, StringName)))
    py_stringname_method = method if isinstance(method, StringName) else c_utils.py_string_to_string_name(method)

    _ret = None
    _ret = self._ptr.call_with_return(25,tuple([py_stringname_method._ptr, *varargs]))
    return _ret


  @functools.native_method
  def set_deferred(self, property:'object'   , value:'object'   ) -> None:
    r'''
				Assigns `value` to the given `property`, at the end of the current frame. This is equivalent to calling `set` through `call_deferred`.
				
				```gdscript
				var node = Node2D.new()
				add_child(node)

				node.rotation = 1.5
				node.set_deferred("rotation", 3.0)
				print(node.rotation) # Prints 1.5

				await get_tree().process_frame
				print(node.rotation) # Prints 3.0
				```
				
				
				**Note:** In C#, `property` must be in snake_case when referring to built-in Godot properties. Prefer using the names exposed in the `PropertyName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not property is None)
    assert isinstance(property, (str, StringName)), 'property must be str or StringName'
    

    if not (type(value).__name__ in py4godot.variant_types.core_types or isinstance(value, Object)):
      raise ValueError(
                f"Unsupported type for argument 'value': "
                f"{type(value).__name__}. "
                "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
                "and built-in types int, float, and bool."
            )





    assert(isinstance(property, (str, StringName)))
    py_stringname_property = property if isinstance(property, StringName) else c_utils.py_string_to_string_name(property)


    self._ptr.call_with_return(26,tuple([py_stringname_property._ptr, value]))

  @functools.native_method
  def callv(self, method:'object'   , arg_array:'Array'   ) -> typing.Union[typing.Any]:
    r'''
				Calls the `method` on the object and returns the result. Unlike `call`, this method expects all parameters to be contained inside `arg_array`.
				
				```gdscript
				var node = Node3D.new()
				node.callv("rotate", `Vector3(1.0, 0.0, 0.0), 1.571`)
				```
				
				
				**Note:** In C#, `method` must be in snake_case when referring to built-in Godot methods. Prefer using the names exposed in the `MethodName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not method is None)
    assert isinstance(method, (str, StringName)), 'method must be str or StringName'
    assert(not arg_array is None)
    assert isinstance(arg_array, Array), 'arg_array must be Array'






    assert(isinstance(method, (str, StringName)))
    py_stringname_method = method if isinstance(method, StringName) else c_utils.py_string_to_string_name(method)


    _ret = None
    _ret = self._ptr.call_with_return(27,tuple([py_stringname_method._ptr, arg_array._ptr]))
    return _ret


  @functools.native_method
  def has_method(self, method:'object'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the given `method` name exists in the object.
				**Note:** In C#, `method` must be in snake_case when referring to built-in Godot methods. Prefer using the names exposed in the `MethodName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not method is None)
    assert isinstance(method, (str, StringName)), 'method must be str or StringName'






    assert(isinstance(method, (str, StringName)))
    py_stringname_method = method if isinstance(method, StringName) else c_utils.py_string_to_string_name(method)

    _ret = 0
    _ret = self._ptr.call_with_return(28,tuple([py_stringname_method._ptr]))
    return _ret


  @functools.native_method
  def get_method_argument_count(self, method:'object'   ) -> typing.Union[int]:
    r'''
				Returns the number of arguments of the given `method` by name.
				**Note:** In C#, `method` must be in snake_case when referring to built-in Godot methods. Prefer using the names exposed in the `MethodName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not method is None)
    assert isinstance(method, (str, StringName)), 'method must be str or StringName'






    assert(isinstance(method, (str, StringName)))
    py_stringname_method = method if isinstance(method, StringName) else c_utils.py_string_to_string_name(method)

    _ret = 0
    _ret = self._ptr.call_with_return(29,tuple([py_stringname_method._ptr]))
    return _ret


  @functools.native_method
  def has_signal(self, signal:'object'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the given `signal` name exists in the object.
				**Note:** In C#, `signal` must be in snake_case when referring to built-in Godot signals. Prefer using the names exposed in the `SignalName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not signal is None)
    assert isinstance(signal, (str, StringName)), 'signal must be str or StringName'






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)

    _ret = 0
    _ret = self._ptr.call_with_return(30,tuple([py_stringname_signal._ptr]))
    return _ret


  @functools.native_method
  def get_signal_list(self) -> typing.Union['py4godot_dictionarytypedarray.DictionaryTypedArray']:
    r'''
				Returns the list of existing signals as an `Array` of dictionaries.
				**Note:** Due to the implementation, each `Dictionary` is formatted very similarly to the returned values of `get_method_list`.
			'''




    _ret = py4godot_dictionarytypedarray.DictionaryTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(31,tuple([]))
    return _ret


  @functools.native_method
  def get_signal_connection_list(self, signal:'object'   ) -> typing.Union['py4godot_dictionarytypedarray.DictionaryTypedArray']:
    r'''
				Returns an `Array` of connections for the given `signal` name. Each connection is represented as a `Dictionary` that contains three entries:
				- `code skip-lint`signal` is a reference to the `Signal`;
				- `callable` is a reference to the connected `Callable`;
				- `flags` is a combination of `enum ConnectFlags`.
			'''

    assert(not signal is None)
    assert isinstance(signal, (str, StringName)), 'signal must be str or StringName'






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)

    _ret = py4godot_dictionarytypedarray.DictionaryTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(32,tuple([py_stringname_signal._ptr]))
    return _ret


  @functools.native_method
  def get_incoming_connections(self) -> typing.Union['py4godot_dictionarytypedarray.DictionaryTypedArray']:
    r'''
				Returns an `Array` of signal connections received by this object. Each connection is represented as a `Dictionary` that contains three entries:
				- `signal` is a reference to the `Signal`;
				- `callable` is a reference to the `Callable`;
				- `flags` is a combination of `enum ConnectFlags`.
			'''




    _ret = py4godot_dictionarytypedarray.DictionaryTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(33,tuple([]))
    return _ret


  @functools.native_method
  def connect(self, signal:'object'   , callable:'Callable'   , flags:'int' =0  ) -> typing.Union[int]:
    r'''
				Connects a `signal` by name to a `callable`. Optional `flags` can be also added to configure the connection's behavior (see `enum ConnectFlags` constants).
				A signal can only be connected once to the same `Callable`. If the signal is already connected, this method returns `constant ERR_INVALID_PARAMETER` and generates an error, unless the signal is connected with `constant CONNECT_REFERENCE_COUNTED`. To prevent this, use `is_connected` first to check for existing connections.
				**Note:** If the `callable`'s object is freed, the connection will be lost.
				**Note:** In GDScript, it is generally recommended to connect signals with `Signal.connect` instead.
				**Note:** This method, and all other signal-related methods, are thread-safe.
			'''

    assert(not signal is None)
    assert(not callable is None)






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)



    _ret:int
    _ret = self._ptr.call_with_return(34,tuple([py_stringname_signal._ptr, callable._ptr, flags]))
    return _ret


  @functools.native_method
  def disconnect(self, signal:'object'   , callable:'Callable'   ) -> None:
    r'''
				Disconnects a `signal` by name from a given `callable`. If the connection does not exist, generates an error. Use `is_connected` to make sure that the connection exists.
			'''

    assert(not signal is None)
    assert(not callable is None)






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)


    self._ptr.call_with_return(35,tuple([py_stringname_signal._ptr, callable._ptr]))

  @functools.native_method
  def is_connected(self, signal:'object'   , callable:'Callable'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if a connection exists between the given `signal` name and `callable`.
				**Note:** In C#, `signal` must be in snake_case when referring to built-in Godot signals. Prefer using the names exposed in the `SignalName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not signal is None)
    assert isinstance(signal, (str, StringName)), 'signal must be str or StringName'
    assert(not callable is None)
    assert isinstance(callable, Callable), 'callable must be Callable'






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)


    _ret = 0
    _ret = self._ptr.call_with_return(36,tuple([py_stringname_signal._ptr, callable._ptr]))
    return _ret


  @functools.native_method
  def has_connections(self, signal:'object'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if any connection exists on the given `signal` name.
				**Note:** In C#, `signal` must be in snake_case when referring to built-in Godot methods. Prefer using the names exposed in the `SignalName` class to avoid allocating a new `StringName` on each call.
			'''

    assert(not signal is None)
    assert isinstance(signal, (str, StringName)), 'signal must be str or StringName'






    assert(isinstance(signal, (str, StringName)))
    py_stringname_signal = signal if isinstance(signal, StringName) else c_utils.py_string_to_string_name(signal)

    _ret = 0
    _ret = self._ptr.call_with_return(37,tuple([py_stringname_signal._ptr]))
    return _ret


  @functools.native_method
  def set_block_signals(self, enable:'bool'   ) -> None:
    r'''
				If set to `true`, the object becomes unable to emit signals. As such, `emit_signal` and signal connections will not work, until it is set to `false`.
			'''

    assert isinstance(enable, bool), 'enable must be bool'








    self._ptr.call_with_return(38,tuple([enable]))

  @functools.native_method
  def is_blocking_signals(self) -> typing.Union[bool]:
    r'''
				Returns `true` if the object is blocking its signals from being emitted. See `set_block_signals`.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(39,tuple([]))
    return _ret


  @functools.native_method
  def notify_property_list_changed(self) -> None:
    r'''
				Emits the `signal property_list_changed` signal. This is mainly used to refresh the editor, so that the Inspector and editor plugins are properly updated.
			'''




    self._ptr.call_with_return(40,tuple([]))

  @functools.native_method
  def set_message_translation(self, enable:'bool'   ) -> None:
    r'''
				If set to `true`, allows the object to translate messages with `tr` and `tr_n`. Enabled by default. See also `can_translate_messages`.
			'''

    assert isinstance(enable, bool), 'enable must be bool'








    self._ptr.call_with_return(41,tuple([enable]))

  @functools.native_method
  def can_translate_messages(self) -> typing.Union[bool]:
    r'''
				Returns `true` if the object is allowed to translate messages with `tr` and `tr_n`. See also `set_message_translation`.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(42,tuple([]))
    return _ret


  @functools.native_method
  def tr(self, message:'object'   , context:'object' = None  ) -> typing.Union[str]:
    r'''
				Translates a `message`, using the translation catalogs configured in the Project Settings. Further `context` can be specified to help with the translation. Note that most `Control` nodes automatically translate their strings, so this method is mostly useful for formatted strings or custom drawn text.
				If `can_translate_messages` is `false`, or no translation is available, this method returns the `message` without changes. See `set_message_translation`.
				For detailed examples, see `url=$DOCS_URL/tutorials/i18n/internationalizing_games.html`Internationalizing games`/url`.
				**Note:** This method can't be used without an `Object` instance, as it requires the `can_translate_messages` method. To translate strings in a static context, use `TranslationServer.translate`.
			'''
    if context is None:
      context = StringName.new0()

    assert(not message is None)
    assert isinstance(message, (str, StringName)), 'message must be str or StringName'






    assert(isinstance(message, (str, StringName)))
    py_stringname_message = message if isinstance(message, StringName) else c_utils.py_string_to_string_name(message)
    assert(isinstance(context, (str, StringName)))
    py_stringname_context = context if isinstance(context, StringName) else c_utils.py_string_to_string_name(context)

    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(43,tuple([py_stringname_message._ptr, py_stringname_context._ptr]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def tr_n(self, message:'object'   , plural_message:'object'   , n:'int'   , context:'object' = None  ) -> typing.Union[str]:
    r'''
				Translates a `message` or `plural_message`, using the translation catalogs configured in the Project Settings. Further `context` can be specified to help with the translation.
				If `can_translate_messages` is `false`, or no translation is available, this method returns `message` or `plural_message`, without changes. See `set_message_translation`.
				The `n` is the number, or amount, of the message's subject. It is used by the translation system to fetch the correct plural form for the current language.
				For detailed examples, see `url=$DOCS_URL/tutorials/i18n/localization_using_gettext.html`Localization using gettext`/url`.
				**Note:** Negative and `float` numbers may not properly apply to some countable subjects. It's recommended to handle these cases with `tr`.
				**Note:** This method can't be used without an `Object` instance, as it requires the `can_translate_messages` method. To translate strings in a static context, use `TranslationServer.translate_plural`.
			'''
    if context is None:
      context = StringName.new0()

    assert(not message is None)
    assert isinstance(message, (str, StringName)), 'message must be str or StringName'
    assert(not plural_message is None)
    assert isinstance(plural_message, (str, StringName)), 'plural_message must be str or StringName'
    assert isinstance(n, (int, float)), 'n must be int or float'






    assert(isinstance(message, (str, StringName)))
    py_stringname_message = message if isinstance(message, StringName) else c_utils.py_string_to_string_name(message)
    assert(isinstance(plural_message, (str, StringName)))
    py_stringname_plural_message = plural_message if isinstance(plural_message, StringName) else c_utils.py_string_to_string_name(plural_message)

    assert(isinstance(context, (str, StringName)))
    py_stringname_context = context if isinstance(context, StringName) else c_utils.py_string_to_string_name(context)

    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(44,tuple([py_stringname_message._ptr, py_stringname_plural_message._ptr, n, py_stringname_context._ptr]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_translation_domain(self) -> typing.Union['StringName']:
    r'''
				Returns the name of the translation domain used by `tr` and `tr_n`. See also `TranslationServer`.
			'''




    _ret = StringName.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(45,tuple([]))
    return _ret


  @functools.native_method
  def set_translation_domain(self, domain:'object'   ) -> None:
    r'''
				Sets the name of the translation domain used by `tr` and `tr_n`. See also `TranslationServer`.
			'''

    assert(not domain is None)
    assert isinstance(domain, (str, StringName)), 'domain must be str or StringName'






    assert(isinstance(domain, (str, StringName)))
    py_stringname_domain = domain if isinstance(domain, StringName) else c_utils.py_string_to_string_name(domain)

    self._ptr.call_with_return(46,tuple([py_stringname_domain._ptr]))

  @functools.native_method
  def is_queued_for_deletion(self) -> typing.Union[bool]:
    r'''
				Returns `true` if the `Node.queue_free` method was called for the object.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(47,tuple([]))
    return _ret


  @functools.native_method
  def cancel_free(self) -> None:
    r'''
				If this method is called during `constant NOTIFICATION_PREDELETE`, this object will reject being freed and will remain allocated. This is mostly an internal function used for error handling to avoid the user from freeing objects when they are not intended to.
			'''




    self._ptr.call_with_return(48,tuple([]))


register_cast_function('Object', Object.cast)
register_class('Object', Object)
