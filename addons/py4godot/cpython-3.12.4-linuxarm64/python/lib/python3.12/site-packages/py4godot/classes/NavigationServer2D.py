# distutils: language=c++
import py4godot.utils.functools as  functools
import py4godot.utils.utils as utils
from py4godot.classes.core import *
from py4godot.utils.smart_cast import smart_cast, register_cast_function, register_class, get_class
from py4godot.utils.CPPWrapper import CPPWrapper, constructor, static_method
import py4godot.utils.utils as c_utils
from py4godot.utils.CoreType import CoreType
import typing
import py4godot.variant_types
import py4godot.classes as classes
import py4godot.classes as classes
import py4godot.signals as signals

import typing
if typing.TYPE_CHECKING:
  pass
  import py4godot.classes.RIDTypedArray as py4godot_ridtypedarray 
  import py4godot.classes.NavigationPolygon as py4godot_navigationpolygon 
  import py4godot.classes.NavigationPathQueryParameters2D as py4godot_navigationpathqueryparameters2d 
  import py4godot.classes.Node as py4godot_node 
  import py4godot.classes.NavigationPathQueryResult2D as py4godot_navigationpathqueryresult2d 
  import py4godot.classes.NavigationMeshSourceGeometryData2D as py4godot_navigationmeshsourcegeometrydata2d 
import py4godot.classes.RIDTypedArray as py4godot_ridtypedarray 
import py4godot.classes.Object as py4godot_object 
from py4godot.wrappers.wrappers import CPPNavigationServer2DWrapper

_NavigationServer2D_singleton_instance = None
class ProcessInfo:
  INFO_ACTIVE_MAPS = 0
  INFO_REGION_COUNT = 1
  INFO_AGENT_COUNT = 2
  INFO_LINK_COUNT = 3
  INFO_POLYGON_COUNT = 4
  INFO_EDGE_COUNT = 5
  INFO_EDGE_MERGE_COUNT = 6
  INFO_EDGE_CONNECTION_COUNT = 7
  INFO_EDGE_FREE_COUNT = 8
  INFO_OBSTACLE_COUNT = 9


class NavigationServer2D(py4godot_object.Object):
  r'''
		NavigationServer2D is the server that handles navigation maps, regions and agents. It does not handle A* navigation from `AStar2D` or `AStarGrid2D`.
		Maps are divided into regions, which are composed of navigation polygons. Together, they define the traversable areas in the 2D world.
		**Note:** Most `NavigationServer2D` changes take effect after the next physics frame and not immediately. This includes all changes made to maps, regions or agents by navigation-related nodes in the scene tree or made through scripts.
		For two regions to be connected to each other, they must share a similar edge. An edge is considered connected to another if both of its two vertices are at a distance less than `edge_connection_margin` to the respective other edge's vertex.
		You may assign navigation layers to regions with `NavigationServer2D.region_set_navigation_layers`, which then can be checked upon when requesting a path with `NavigationServer2D.map_get_path`. This can be used to allow or deny certain areas for some objects.
		To use the collision avoidance system, you may use agents. You can set an agent's target velocity, then the servers will emit a callback with a modified velocity.
		**Note:** The collision avoidance system ignores regions. Using the modified velocity directly may move an agent outside of the traversable area. This is a limitation of the collision avoidance system, any more complex situation may require the use of the physics engine.
		This server keeps tracks of any call and executes them during the sync phase. This means that you can request any change to the map, using any thread, without worrying.
	'''


  def generate_wrapper(self):
    return CPPNavigationServer2DWrapper()
  def init_signals(self):
    super().init_signals()
    map_changed_name = utils.py_string_to_string_name("map_changed")
    self.map_changed = signals.BuiltinSignal(self, map_changed_name)
    navigation_debug_changed_name = utils.py_string_to_string_name("navigation_debug_changed")
    self.navigation_debug_changed = signals.BuiltinSignal(self, navigation_debug_changed_name)
    avoidance_debug_changed_name = utils.py_string_to_string_name("avoidance_debug_changed")
    self.avoidance_debug_changed = signals.BuiltinSignal(self, avoidance_debug_changed_name)

  @staticmethod
  def construct_without_init():
    cls = NavigationServer2D.__new__(NavigationServer2D)
    cls.shouldBeDeleted = True
    cls.casted_from = None
    cls.init_signals()
    return cls
  @staticmethod
  def get_type():
    return 'NavigationServer2D'

  @staticmethod
  def cast(other:'Object'):
    assert other != None # Object to be casted must not be None
    cls = NavigationServer2D.construct_without_init()
    cls._ptr = CPPNavigationServer2DWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls
  @staticmethod
  def cast_without_reference(other: 'Object'):
    assert other != None # Object to be casted must not be None
    cls = NavigationServer2D.construct_without_init()
    cls._ptr = CPPNavigationServer2DWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls
  @staticmethod
  def instance():
    global _NavigationServer2D_singleton_instance
    if _NavigationServer2D_singleton_instance is None:
      singleton = NavigationServer2D()
      singleton._ptr = constructor(511,0, ())
      _NavigationServer2D_singleton_instance = singleton
    return _NavigationServer2D_singleton_instance

  @functools.native_method
  def get_maps(self) -> typing.Union['py4godot_ridtypedarray.RIDTypedArray']:
    r'''
				Returns all created navigation map `RID`s on the NavigationServer. This returns both 2D and 3D created navigation maps as there is technically no distinction between them.
			'''




    _ret = py4godot_ridtypedarray.RIDTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(49,tuple([]))
    return _ret


  @functools.native_method
  def map_create(self) -> typing.Union['RID']:
    r'''
				Create a new map.
			'''




    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(50,tuple([]))
    return _ret


  @functools.native_method
  def map_set_active(self, map:'RID'   , active:'bool'   ) -> None:
    r'''
				Sets the map active.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(active, bool), 'active must be bool'









    self._ptr.call_with_return(51,tuple([map._ptr, active]))

  @functools.native_method
  def map_is_active(self, map:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the map is active.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(52,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_set_cell_size(self, map:'RID'   , cell_size:'float'   ) -> None:
    r'''
				Sets the map cell size used to rasterize the navigation mesh vertices. Must match with the cell size of the used navigation meshes.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(cell_size, (int, float)), 'cell_size must be int or float'









    self._ptr.call_with_return(53,tuple([map._ptr, cell_size]))

  @functools.native_method
  def map_get_cell_size(self, map:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the map cell size used to rasterize the navigation mesh vertices.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(54,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_set_merge_rasterizer_cell_scale(self, map:'RID'   , scale:'float'   ) -> None:
    r'''
				Set the map's internal merge rasterizer cell scale used to control merging sensitivity.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(scale, (int, float)), 'scale must be int or float'









    self._ptr.call_with_return(55,tuple([map._ptr, scale]))

  @functools.native_method
  def map_get_merge_rasterizer_cell_scale(self, map:'RID'   ) -> typing.Union[float]:
    r'''
				Returns map's internal merge rasterizer cell scale.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(56,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_set_use_edge_connections(self, map:'RID'   , enabled:'bool'   ) -> None:
    r'''
				Set the navigation `map` edge connection use. If `enabled` is `true`, the navigation map allows navigation regions to use edge connections to connect with other navigation regions within proximity of the navigation map edge connection margin.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(57,tuple([map._ptr, enabled]))

  @functools.native_method
  def map_get_use_edge_connections(self, map:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns whether the navigation `map` allows navigation regions to use edge connections to connect with other navigation regions within proximity of the navigation map edge connection margin.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(58,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_set_edge_connection_margin(self, map:'RID'   , margin:'float'   ) -> None:
    r'''
				Set the map edge connection margin used to weld the compatible region edges.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(margin, (int, float)), 'margin must be int or float'









    self._ptr.call_with_return(59,tuple([map._ptr, margin]))

  @functools.native_method
  def map_get_edge_connection_margin(self, map:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the edge connection margin of the map. The edge connection margin is a distance used to connect two regions.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(60,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_set_link_connection_radius(self, map:'RID'   , radius:'float'   ) -> None:
    r'''
				Set the map's link connection radius used to connect links to navigation polygons.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(radius, (int, float)), 'radius must be int or float'









    self._ptr.call_with_return(61,tuple([map._ptr, radius]))

  @functools.native_method
  def map_get_link_connection_radius(self, map:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the link connection radius of the map. This distance is the maximum range any link will search for navigation mesh polygons to connect to.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(62,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_get_path(self, map:'RID'   , origin:'Vector2'   , destination:'Vector2'   , optimize:'bool'   , navigation_layers:'int' =1  ) -> typing.Union['PackedVector2Array']:
    r'''
				Returns the navigation path to reach the destination from the origin. `navigation_layers` is a bitmask of all region navigation layers that are allowed to be in the path.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert(not origin is None)
    assert isinstance(origin, Vector2), 'origin must be Vector2'
    assert(not destination is None)
    assert isinstance(destination, Vector2), 'destination must be Vector2'
    assert isinstance(optimize, bool), 'optimize must be bool'
    assert isinstance(navigation_layers, (int, float)), 'navigation_layers must be int or float'












    _ret = PackedVector2Array.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(63,tuple([map._ptr, origin._ptr, destination._ptr, optimize, navigation_layers]))
    return _ret


  @functools.native_method
  def map_get_closest_point(self, map:'RID'   , to_point:'Vector2'   ) -> typing.Union['Vector2']:
    r'''
				Returns the navigation mesh surface point closest to the provided `to_point` on the navigation `map`.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert(not to_point is None)
    assert isinstance(to_point, Vector2), 'to_point must be Vector2'









    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(64,tuple([map._ptr, to_point._ptr]))
    return _ret


  @functools.native_method
  def map_get_closest_point_owner(self, map:'RID'   , to_point:'Vector2'   ) -> typing.Union['RID']:
    r'''
				Returns the owner region RID for the navigation mesh surface point closest to the provided `to_point` on the navigation `map`.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert(not to_point is None)
    assert isinstance(to_point, Vector2), 'to_point must be Vector2'









    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(65,tuple([map._ptr, to_point._ptr]))
    return _ret


  @functools.native_method
  def map_get_links(self, map:'RID'   ) -> typing.Union['py4godot_ridtypedarray.RIDTypedArray']:
    r'''
				Returns all navigation link `RID`s that are currently assigned to the requested navigation `map`.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = py4godot_ridtypedarray.RIDTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(66,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_get_regions(self, map:'RID'   ) -> typing.Union['py4godot_ridtypedarray.RIDTypedArray']:
    r'''
				Returns all navigation regions `RID`s that are currently assigned to the requested navigation `map`.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = py4godot_ridtypedarray.RIDTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(67,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_get_agents(self, map:'RID'   ) -> typing.Union['py4godot_ridtypedarray.RIDTypedArray']:
    r'''
				Returns all navigation agents `RID`s that are currently assigned to the requested navigation `map`.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = py4godot_ridtypedarray.RIDTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(68,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_get_obstacles(self, map:'RID'   ) -> typing.Union['py4godot_ridtypedarray.RIDTypedArray']:
    r'''
				Returns all navigation obstacle `RID`s that are currently assigned to the requested navigation `map`.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = py4godot_ridtypedarray.RIDTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(69,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_force_update(self, map:'RID'   ) -> None:
    r'''
				This function immediately forces synchronization of the specified navigation `map` `RID`. By default navigation maps are only synchronized at the end of each physics frame. This function can be used to immediately (re)calculate all the navigation meshes and region connections of the navigation map. This makes it possible to query a navigation path for a changed map immediately and in the same frame (multiple times if needed).
				Due to technical restrictions the current NavigationServer command queue will be flushed. This means all already queued update commands for this physics frame will be executed, even those intended for other maps, regions and agents not part of the specified map. The expensive computation of the navigation meshes and region connections of a map will only be done for the specified map. Other maps will receive the normal synchronization at the end of the physics frame. Should the specified map receive changes after the forced update it will update again as well when the other maps receive their update.
				Avoidance processing and dispatch of the `safe_velocity` signals is unaffected by this function and continues to happen for all maps and agents at the end of the physics frame.
				**Note:** With great power comes great responsibility. This function should only be used by users that really know what they are doing and have a good reason for it. Forcing an immediate update of a navigation map requires locking the NavigationServer and flushing the entire NavigationServer command queue. Not only can this severely impact the performance of a game but it can also introduce bugs if used inappropriately without much foresight.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    self._ptr.call_with_return(70,tuple([map._ptr]))

  @functools.native_method
  def map_get_iteration_id(self, map:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the current iteration id of the navigation map. Every time the navigation map changes and synchronizes the iteration id increases. An iteration id of 0 means the navigation map has never synchronized.
				**Note:** The iteration id will wrap back to 1 after reaching its range limit.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(71,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_set_use_async_iterations(self, map:'RID'   , enabled:'bool'   ) -> None:
    r'''
				If `enabled` is `true` the `map` synchronization uses an async process that runs on a background thread.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(72,tuple([map._ptr, enabled]))

  @functools.native_method
  def map_get_use_async_iterations(self, map:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the `map` synchronization uses an async process that runs on a background thread.
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(73,tuple([map._ptr]))
    return _ret


  @functools.native_method
  def map_get_random_point(self, map:'RID'   , navigation_layers:'int'   , uniformly:'bool'   ) -> typing.Union['Vector2']:
    r'''
				Returns a random position picked from all map region polygons with matching `navigation_layers`.
				If `uniformly` is `true`, all map regions, polygons, and faces are weighted by their surface area (slower).
				If `uniformly` is `false`, just a random region and a random polygon are picked (faster).
			'''

    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'
    assert isinstance(navigation_layers, (int, float)), 'navigation_layers must be int or float'
    assert isinstance(uniformly, bool), 'uniformly must be bool'










    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(74,tuple([map._ptr, navigation_layers, uniformly]))
    return _ret


  @functools.native_method
  def query_path(self, parameters:'py4godot_navigationpathqueryparameters2d.NavigationPathQueryParameters2D'   , result:'py4godot_navigationpathqueryresult2d.NavigationPathQueryResult2D'   , callback:'Callable' = None  ) -> None:
    r'''
				Queries a path in a given navigation map. Start and target position and other parameters are defined through `NavigationPathQueryParameters2D`. Updates the provided `NavigationPathQueryResult2D` result object with the path among other results requested by the query. After the process is finished the optional `callback` will be called.
			'''
    if callback is None:
      callback = Callable.new0()

    assert(not parameters is None)
    assert isinstance(parameters, get_class('NavigationPathQueryParameters2D')), 'parameters must be NavigationPathQueryParameters2D'
    assert(not result is None)
    assert isinstance(result, get_class('NavigationPathQueryResult2D')), 'result must be NavigationPathQueryResult2D'


    parameters.reference()
    result.reference()








    self._ptr.call_with_return(75,tuple([parameters._ptr, result._ptr, callback._ptr]))

  @functools.native_method
  def region_create(self) -> typing.Union['RID']:
    r'''
				Creates a new region.
			'''




    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(76,tuple([]))
    return _ret


  @functools.native_method
  def region_get_iteration_id(self, region:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the current iteration ID of the navigation region. Every time the navigation region changes and synchronizes, the iteration ID increases. An iteration ID of `0` means the navigation region has never synchronized.
				**Note:** The iteration ID will wrap around to `1` after reaching its range limit.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(77,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_use_async_iterations(self, region:'RID'   , enabled:'bool'   ) -> None:
    r'''
				If `enabled` is `true` the `region` uses an async synchronization process that runs on a background thread.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(78,tuple([region._ptr, enabled]))

  @functools.native_method
  def region_get_use_async_iterations(self, region:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the `region` uses an async synchronization process that runs on a background thread.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(79,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_enabled(self, region:'RID'   , enabled:'bool'   ) -> None:
    r'''
				If `enabled` is `true` the specified `region` will contribute to its current navigation map.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(80,tuple([region._ptr, enabled]))

  @functools.native_method
  def region_get_enabled(self, region:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the specified `region` is enabled.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(81,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_use_edge_connections(self, region:'RID'   , enabled:'bool'   ) -> None:
    r'''
				If `enabled` is `true`, the navigation `region` will use edge connections to connect with other navigation regions within proximity of the navigation map edge connection margin.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(82,tuple([region._ptr, enabled]))

  @functools.native_method
  def region_get_use_edge_connections(self, region:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns whether the navigation `region` is set to use edge connections to connect with other navigation regions within proximity of the navigation map edge connection margin.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(83,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_enter_cost(self, region:'RID'   , enter_cost:'float'   ) -> None:
    r'''
				Sets the `enter_cost` for this `region`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(enter_cost, (int, float)), 'enter_cost must be int or float'









    self._ptr.call_with_return(84,tuple([region._ptr, enter_cost]))

  @functools.native_method
  def region_get_enter_cost(self, region:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the enter cost of this `region`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(85,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_travel_cost(self, region:'RID'   , travel_cost:'float'   ) -> None:
    r'''
				Sets the `travel_cost` for this `region`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(travel_cost, (int, float)), 'travel_cost must be int or float'









    self._ptr.call_with_return(86,tuple([region._ptr, travel_cost]))

  @functools.native_method
  def region_get_travel_cost(self, region:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the travel cost of this `region`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(87,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_owner_id(self, region:'RID'   , owner_id:'int'   ) -> None:
    r'''
				Set the `ObjectID` of the object which manages this region.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(owner_id, (int, float)), 'owner_id must be int or float'









    self._ptr.call_with_return(88,tuple([region._ptr, owner_id]))

  @functools.native_method
  def region_get_owner_id(self, region:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the `ObjectID` of the object which manages this region.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(89,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_owns_point(self, region:'RID'   , point:'Vector2'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the provided `point` in world space is currently owned by the provided navigation `region`. Owned in this context means that one of the region's navigation mesh polygon faces has a possible position at the closest distance to this point compared to all other navigation meshes from other navigation regions that are also registered on the navigation map of the provided region.
				If multiple navigation meshes have positions at equal distance the navigation region whose polygons are processed first wins the ownership. Polygons are processed in the same order that navigation regions were registered on the NavigationServer.
				**Note:** If navigation meshes from different navigation regions overlap (which should be avoided in general) the result might not be what is expected.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert(not point is None)
    assert isinstance(point, Vector2), 'point must be Vector2'









    _ret = 0
    _ret = self._ptr.call_with_return(90,tuple([region._ptr, point._ptr]))
    return _ret


  @functools.native_method
  def region_set_map(self, region:'RID'   , map:'RID'   ) -> None:
    r'''
				Sets the map for the region.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'









    self._ptr.call_with_return(91,tuple([region._ptr, map._ptr]))

  @functools.native_method
  def region_get_map(self, region:'RID'   ) -> typing.Union['RID']:
    r'''
				Returns the navigation map `RID` the requested `region` is currently assigned to.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(92,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_navigation_layers(self, region:'RID'   , navigation_layers:'int'   ) -> None:
    r'''
				Set the region's navigation layers. This allows selecting regions from a path request (when using `NavigationServer2D.map_get_path`).
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(navigation_layers, (int, float)), 'navigation_layers must be int or float'









    self._ptr.call_with_return(93,tuple([region._ptr, navigation_layers]))

  @functools.native_method
  def region_get_navigation_layers(self, region:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the region's navigation layers.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(94,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_transform(self, region:'RID'   , transform:'Transform2D'   ) -> None:
    r'''
				Sets the global transformation for the region.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert(not transform is None)
    assert isinstance(transform, Transform2D), 'transform must be Transform2D'









    self._ptr.call_with_return(95,tuple([region._ptr, transform._ptr]))

  @functools.native_method
  def region_get_transform(self, region:'RID'   ) -> typing.Union['Transform2D']:
    r'''
				Returns the global transformation of this `region`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = Transform2D.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(96,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_set_navigation_polygon(self, region:'RID'   , navigation_polygon:'py4godot_navigationpolygon.NavigationPolygon'   ) -> None:
    r'''
				Sets the `navigation_polygon` for the region.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert(not navigation_polygon is None)
    assert isinstance(navigation_polygon, get_class('NavigationPolygon')), 'navigation_polygon must be NavigationPolygon'


    navigation_polygon.reference()







    self._ptr.call_with_return(97,tuple([region._ptr, navigation_polygon._ptr]))

  @functools.native_method
  def region_get_connections_count(self, region:'RID'   ) -> typing.Union[int]:
    r'''
				Returns how many connections this `region` has with other regions in the map.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(98,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def region_get_connection_pathway_start(self, region:'RID'   , connection:'int'   ) -> typing.Union['Vector2']:
    r'''
				Returns the starting point of a connection door. `connection` is an index between 0 and the return value of `region_get_connections_count`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(connection, (int, float)), 'connection must be int or float'









    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(99,tuple([region._ptr, connection]))
    return _ret


  @functools.native_method
  def region_get_connection_pathway_end(self, region:'RID'   , connection:'int'   ) -> typing.Union['Vector2']:
    r'''
				Returns the ending point of a connection door. `connection` is an index between 0 and the return value of `region_get_connections_count`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(connection, (int, float)), 'connection must be int or float'









    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(100,tuple([region._ptr, connection]))
    return _ret


  @functools.native_method
  def region_get_closest_point(self, region:'RID'   , to_point:'Vector2'   ) -> typing.Union['Vector2']:
    r'''
				Returns the navigation mesh surface point closest to the provided `to_point` on the navigation `region`.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert(not to_point is None)
    assert isinstance(to_point, Vector2), 'to_point must be Vector2'









    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(101,tuple([region._ptr, to_point._ptr]))
    return _ret


  @functools.native_method
  def region_get_random_point(self, region:'RID'   , navigation_layers:'int'   , uniformly:'bool'   ) -> typing.Union['Vector2']:
    r'''
				Returns a random position picked from all region polygons with matching `navigation_layers`.
				If `uniformly` is `true`, all region polygons and faces are weighted by their surface area (slower).
				If `uniformly` is `false`, just a random polygon and face is picked (faster).
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'
    assert isinstance(navigation_layers, (int, float)), 'navigation_layers must be int or float'
    assert isinstance(uniformly, bool), 'uniformly must be bool'










    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(102,tuple([region._ptr, navigation_layers, uniformly]))
    return _ret


  @functools.native_method
  def region_get_bounds(self, region:'RID'   ) -> typing.Union['Rect2']:
    r'''
				Returns the axis-aligned rectangle for the `region`'s transformed navigation mesh.
			'''

    assert(not region is None)
    assert isinstance(region, RID), 'region must be RID'








    _ret = Rect2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(103,tuple([region._ptr]))
    return _ret


  @functools.native_method
  def link_create(self) -> typing.Union['RID']:
    r'''
				Create a new link between two positions on a map.
			'''




    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(104,tuple([]))
    return _ret


  @functools.native_method
  def link_get_iteration_id(self, link:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the current iteration ID of the navigation link. Every time the navigation link changes and synchronizes, the iteration ID increases. An iteration ID of `0` means the navigation link has never synchronized.
				**Note:** The iteration ID will wrap around to `1` after reaching its range limit.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(105,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_map(self, link:'RID'   , map:'RID'   ) -> None:
    r'''
				Sets the navigation map `RID` for the link.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'









    self._ptr.call_with_return(106,tuple([link._ptr, map._ptr]))

  @functools.native_method
  def link_get_map(self, link:'RID'   ) -> typing.Union['RID']:
    r'''
				Returns the navigation map `RID` the requested `link` is currently assigned to.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(107,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_enabled(self, link:'RID'   , enabled:'bool'   ) -> None:
    r'''
				If `enabled` is `true`, the specified `link` will contribute to its current navigation map.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(108,tuple([link._ptr, enabled]))

  @functools.native_method
  def link_get_enabled(self, link:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the specified `link` is enabled.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(109,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_bidirectional(self, link:'RID'   , bidirectional:'bool'   ) -> None:
    r'''
				Sets whether this `link` can be travelled in both directions.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert isinstance(bidirectional, bool), 'bidirectional must be bool'









    self._ptr.call_with_return(110,tuple([link._ptr, bidirectional]))

  @functools.native_method
  def link_is_bidirectional(self, link:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns whether this `link` can be travelled in both directions.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(111,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_navigation_layers(self, link:'RID'   , navigation_layers:'int'   ) -> None:
    r'''
				Set the links's navigation layers. This allows selecting links from a path request (when using `NavigationServer2D.map_get_path`).
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert isinstance(navigation_layers, (int, float)), 'navigation_layers must be int or float'









    self._ptr.call_with_return(112,tuple([link._ptr, navigation_layers]))

  @functools.native_method
  def link_get_navigation_layers(self, link:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the navigation layers for this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(113,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_start_position(self, link:'RID'   , position:'Vector2'   ) -> None:
    r'''
				Sets the entry position for this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert(not position is None)
    assert isinstance(position, Vector2), 'position must be Vector2'









    self._ptr.call_with_return(114,tuple([link._ptr, position._ptr]))

  @functools.native_method
  def link_get_start_position(self, link:'RID'   ) -> typing.Union['Vector2']:
    r'''
				Returns the starting position of this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(115,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_end_position(self, link:'RID'   , position:'Vector2'   ) -> None:
    r'''
				Sets the exit position for the `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert(not position is None)
    assert isinstance(position, Vector2), 'position must be Vector2'









    self._ptr.call_with_return(116,tuple([link._ptr, position._ptr]))

  @functools.native_method
  def link_get_end_position(self, link:'RID'   ) -> typing.Union['Vector2']:
    r'''
				Returns the ending position of this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(117,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_enter_cost(self, link:'RID'   , enter_cost:'float'   ) -> None:
    r'''
				Sets the `enter_cost` for this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert isinstance(enter_cost, (int, float)), 'enter_cost must be int or float'









    self._ptr.call_with_return(118,tuple([link._ptr, enter_cost]))

  @functools.native_method
  def link_get_enter_cost(self, link:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the enter cost of this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(119,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_travel_cost(self, link:'RID'   , travel_cost:'float'   ) -> None:
    r'''
				Sets the `travel_cost` for this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert isinstance(travel_cost, (int, float)), 'travel_cost must be int or float'









    self._ptr.call_with_return(120,tuple([link._ptr, travel_cost]))

  @functools.native_method
  def link_get_travel_cost(self, link:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the travel cost of this `link`.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(121,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def link_set_owner_id(self, link:'RID'   , owner_id:'int'   ) -> None:
    r'''
				Set the `ObjectID` of the object which manages this link.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'
    assert isinstance(owner_id, (int, float)), 'owner_id must be int or float'









    self._ptr.call_with_return(122,tuple([link._ptr, owner_id]))

  @functools.native_method
  def link_get_owner_id(self, link:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the `ObjectID` of the object which manages this link.
			'''

    assert(not link is None)
    assert isinstance(link, RID), 'link must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(123,tuple([link._ptr]))
    return _ret


  @functools.native_method
  def agent_create(self) -> typing.Union['RID']:
    r'''
				Creates the agent.
			'''




    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(124,tuple([]))
    return _ret


  @functools.native_method
  def agent_set_avoidance_enabled(self, agent:'RID'   , enabled:'bool'   ) -> None:
    r'''
				If `enabled` is `true`, the specified `agent` uses avoidance.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(125,tuple([agent._ptr, enabled]))

  @functools.native_method
  def agent_get_avoidance_enabled(self, agent:'RID'   ) -> typing.Union[bool]:
    r'''
				Return `true` if the specified `agent` uses avoidance.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(126,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_map(self, agent:'RID'   , map:'RID'   ) -> None:
    r'''
				Puts the agent in the map.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'









    self._ptr.call_with_return(127,tuple([agent._ptr, map._ptr]))

  @functools.native_method
  def agent_get_map(self, agent:'RID'   ) -> typing.Union['RID']:
    r'''
				Returns the navigation map `RID` the requested `agent` is currently assigned to.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(128,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_paused(self, agent:'RID'   , paused:'bool'   ) -> None:
    r'''
				If `paused` is `true` the specified `agent` will not be processed. For example, it will not calculate avoidance velocities or receive avoidance callbacks.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(paused, bool), 'paused must be bool'









    self._ptr.call_with_return(129,tuple([agent._ptr, paused]))

  @functools.native_method
  def agent_get_paused(self, agent:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the specified `agent` is paused.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(130,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_neighbor_distance(self, agent:'RID'   , distance:'float'   ) -> None:
    r'''
				Sets the maximum distance to other agents this agent takes into account in the navigation. The larger this number, the longer the running time of the simulation. If the number is too low, the simulation will not be safe.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(distance, (int, float)), 'distance must be int or float'









    self._ptr.call_with_return(131,tuple([agent._ptr, distance]))

  @functools.native_method
  def agent_get_neighbor_distance(self, agent:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the maximum distance to other agents the specified `agent` takes into account in the navigation.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(132,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_max_neighbors(self, agent:'RID'   , count:'int'   ) -> None:
    r'''
				Sets the maximum number of other agents the agent takes into account in the navigation. The larger this number, the longer the running time of the simulation. If the number is too low, the simulation will not be safe.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(count, (int, float)), 'count must be int or float'









    self._ptr.call_with_return(133,tuple([agent._ptr, count]))

  @functools.native_method
  def agent_get_max_neighbors(self, agent:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the maximum number of other agents the specified `agent` takes into account in the navigation.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(134,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_time_horizon_agents(self, agent:'RID'   , time_horizon:'float'   ) -> None:
    r'''
				The minimal amount of time for which the agent's velocities that are computed by the simulation are safe with respect to other agents. The larger this number, the sooner this agent will respond to the presence of other agents, but the less freedom this agent has in choosing its velocities. A too high value will slow down agents movement considerably. Must be positive.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(time_horizon, (int, float)), 'time_horizon must be int or float'









    self._ptr.call_with_return(135,tuple([agent._ptr, time_horizon]))

  @functools.native_method
  def agent_get_time_horizon_agents(self, agent:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the minimal amount of time for which the specified `agent`'s velocities that are computed by the simulation are safe with respect to other agents.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(136,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_time_horizon_obstacles(self, agent:'RID'   , time_horizon:'float'   ) -> None:
    r'''
				The minimal amount of time for which the agent's velocities that are computed by the simulation are safe with respect to static avoidance obstacles. The larger this number, the sooner this agent will respond to the presence of static avoidance obstacles, but the less freedom this agent has in choosing its velocities. A too high value will slow down agents movement considerably. Must be positive.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(time_horizon, (int, float)), 'time_horizon must be int or float'









    self._ptr.call_with_return(137,tuple([agent._ptr, time_horizon]))

  @functools.native_method
  def agent_get_time_horizon_obstacles(self, agent:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the minimal amount of time for which the specified `agent`'s velocities that are computed by the simulation are safe with respect to static avoidance obstacles.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(138,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_radius(self, agent:'RID'   , radius:'float'   ) -> None:
    r'''
				Sets the radius of the agent.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(radius, (int, float)), 'radius must be int or float'









    self._ptr.call_with_return(139,tuple([agent._ptr, radius]))

  @functools.native_method
  def agent_get_radius(self, agent:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the radius of the specified `agent`.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(140,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_max_speed(self, agent:'RID'   , max_speed:'float'   ) -> None:
    r'''
				Sets the maximum speed of the agent. Must be positive.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(max_speed, (int, float)), 'max_speed must be int or float'









    self._ptr.call_with_return(141,tuple([agent._ptr, max_speed]))

  @functools.native_method
  def agent_get_max_speed(self, agent:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the maximum speed of the specified `agent`.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(142,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_velocity_forced(self, agent:'RID'   , velocity:'Vector2'   ) -> None:
    r'''
				Replaces the internal velocity in the collision avoidance simulation with `velocity` for the specified `agent`. When an agent is teleported to a new position far away this function should be used in the same frame. If called frequently this function can get agents stuck.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert(not velocity is None)
    assert isinstance(velocity, Vector2), 'velocity must be Vector2'









    self._ptr.call_with_return(143,tuple([agent._ptr, velocity._ptr]))

  @functools.native_method
  def agent_set_velocity(self, agent:'RID'   , velocity:'Vector2'   ) -> None:
    r'''
				Sets `velocity` as the new wanted velocity for the specified `agent`. The avoidance simulation will try to fulfill this velocity if possible but will modify it to avoid collision with other agent's and obstacles. When an agent is teleported to a new position far away use `agent_set_velocity_forced` instead to reset the internal velocity state.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert(not velocity is None)
    assert isinstance(velocity, Vector2), 'velocity must be Vector2'









    self._ptr.call_with_return(144,tuple([agent._ptr, velocity._ptr]))

  @functools.native_method
  def agent_get_velocity(self, agent:'RID'   ) -> typing.Union['Vector2']:
    r'''
				Returns the velocity of the specified `agent`.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(145,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_position(self, agent:'RID'   , position:'Vector2'   ) -> None:
    r'''
				Sets the position of the agent in world space.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert(not position is None)
    assert isinstance(position, Vector2), 'position must be Vector2'









    self._ptr.call_with_return(146,tuple([agent._ptr, position._ptr]))

  @functools.native_method
  def agent_get_position(self, agent:'RID'   ) -> typing.Union['Vector2']:
    r'''
				Returns the position of the specified `agent` in world space.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(147,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_is_map_changed(self, agent:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the map got changed the previous frame.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(148,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_avoidance_callback(self, agent:'RID'   , callback:'Callable'   ) -> None:
    r'''
				Sets the callback `Callable` that gets called after each avoidance processing step for the `agent`. The calculated `safe_velocity` will be dispatched with a signal to the object just before the physics calculations.
				**Note:** Created callbacks are always processed independently of the SceneTree state as long as the agent is on a navigation map and not freed. To disable the dispatch of a callback from an agent use `agent_set_avoidance_callback` again with an empty `Callable`.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert(not callback is None)
    assert isinstance(callback, Callable), 'callback must be Callable'









    self._ptr.call_with_return(149,tuple([agent._ptr, callback._ptr]))

  @functools.native_method
  def agent_has_avoidance_callback(self, agent:'RID'   ) -> typing.Union[bool]:
    r'''
				Return `true` if the specified `agent` has an avoidance callback.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(150,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_avoidance_layers(self, agent:'RID'   , layers:'int'   ) -> None:
    r'''
				Set the agent's `avoidance_layers` bitmask.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(layers, (int, float)), 'layers must be int or float'









    self._ptr.call_with_return(151,tuple([agent._ptr, layers]))

  @functools.native_method
  def agent_get_avoidance_layers(self, agent:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the `avoidance_layers` bitmask of the specified `agent`.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(152,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_avoidance_mask(self, agent:'RID'   , mask:'int'   ) -> None:
    r'''
				Set the agent's `avoidance_mask` bitmask.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(mask, (int, float)), 'mask must be int or float'









    self._ptr.call_with_return(153,tuple([agent._ptr, mask]))

  @functools.native_method
  def agent_get_avoidance_mask(self, agent:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the `avoidance_mask` bitmask of the specified `agent`.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(154,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def agent_set_avoidance_priority(self, agent:'RID'   , priority:'float'   ) -> None:
    r'''
				Set the agent's `avoidance_priority` with a `priority` between 0.0 (lowest priority) to 1.0 (highest priority).
				The specified `agent` does not adjust the velocity for other agents that would match the `avoidance_mask` but have a lower `avoidance_priority`. This in turn makes the other agents with lower priority adjust their velocities even more to avoid collision with this agent.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'
    assert isinstance(priority, (int, float)), 'priority must be int or float'









    self._ptr.call_with_return(155,tuple([agent._ptr, priority]))

  @functools.native_method
  def agent_get_avoidance_priority(self, agent:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the `avoidance_priority` of the specified `agent`.
			'''

    assert(not agent is None)
    assert isinstance(agent, RID), 'agent must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(156,tuple([agent._ptr]))
    return _ret


  @functools.native_method
  def obstacle_create(self) -> typing.Union['RID']:
    r'''
				Creates a new navigation obstacle.
			'''




    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(157,tuple([]))
    return _ret


  @functools.native_method
  def obstacle_set_avoidance_enabled(self, obstacle:'RID'   , enabled:'bool'   ) -> None:
    r'''
				If `enabled` is `true`, the provided `obstacle` affects avoidance using agents.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert isinstance(enabled, bool), 'enabled must be bool'









    self._ptr.call_with_return(158,tuple([obstacle._ptr, enabled]))

  @functools.native_method
  def obstacle_get_avoidance_enabled(self, obstacle:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the provided `obstacle` has avoidance enabled.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(159,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def obstacle_set_map(self, obstacle:'RID'   , map:'RID'   ) -> None:
    r'''
				Sets the navigation map `RID` for the obstacle.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert(not map is None)
    assert isinstance(map, RID), 'map must be RID'









    self._ptr.call_with_return(160,tuple([obstacle._ptr, map._ptr]))

  @functools.native_method
  def obstacle_get_map(self, obstacle:'RID'   ) -> typing.Union['RID']:
    r'''
				Returns the navigation map `RID` the requested `obstacle` is currently assigned to.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(161,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def obstacle_set_paused(self, obstacle:'RID'   , paused:'bool'   ) -> None:
    r'''
				If `paused` is `true` the specified `obstacle` will not be processed. For example, it will no longer affect avoidance velocities.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert isinstance(paused, bool), 'paused must be bool'









    self._ptr.call_with_return(162,tuple([obstacle._ptr, paused]))

  @functools.native_method
  def obstacle_get_paused(self, obstacle:'RID'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the specified `obstacle` is paused.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(163,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def obstacle_set_radius(self, obstacle:'RID'   , radius:'float'   ) -> None:
    r'''
				Sets the radius of the dynamic obstacle.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert isinstance(radius, (int, float)), 'radius must be int or float'









    self._ptr.call_with_return(164,tuple([obstacle._ptr, radius]))

  @functools.native_method
  def obstacle_get_radius(self, obstacle:'RID'   ) -> typing.Union[float]:
    r'''
				Returns the radius of the specified dynamic `obstacle`.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(165,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def obstacle_set_velocity(self, obstacle:'RID'   , velocity:'Vector2'   ) -> None:
    r'''
				Sets `velocity` of the dynamic `obstacle`. Allows other agents to better predict the movement of the dynamic obstacle. Only works in combination with the radius of the obstacle.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert(not velocity is None)
    assert isinstance(velocity, Vector2), 'velocity must be Vector2'









    self._ptr.call_with_return(166,tuple([obstacle._ptr, velocity._ptr]))

  @functools.native_method
  def obstacle_get_velocity(self, obstacle:'RID'   ) -> typing.Union['Vector2']:
    r'''
				Returns the velocity of the specified dynamic `obstacle`.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(167,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def obstacle_set_position(self, obstacle:'RID'   , position:'Vector2'   ) -> None:
    r'''
				Sets the position of the obstacle in world space.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert(not position is None)
    assert isinstance(position, Vector2), 'position must be Vector2'









    self._ptr.call_with_return(168,tuple([obstacle._ptr, position._ptr]))

  @functools.native_method
  def obstacle_get_position(self, obstacle:'RID'   ) -> typing.Union['Vector2']:
    r'''
				Returns the position of the specified `obstacle` in world space.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(169,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def obstacle_set_vertices(self, obstacle:'RID'   , vertices:'PackedVector2Array'   ) -> None:
    r'''
				Sets the outline vertices for the obstacle. If the vertices are winded in clockwise order agents will be pushed in by the obstacle, else they will be pushed out.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert(not vertices is None)
    assert isinstance(vertices, PackedVector2Array), 'vertices must be PackedVector2Array'









    self._ptr.call_with_return(170,tuple([obstacle._ptr, vertices._ptr]))

  @functools.native_method
  def obstacle_get_vertices(self, obstacle:'RID'   ) -> typing.Union['PackedVector2Array']:
    r'''
				Returns the outline vertices for the specified `obstacle`.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = PackedVector2Array.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(171,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def obstacle_set_avoidance_layers(self, obstacle:'RID'   , layers:'int'   ) -> None:
    r'''
				Set the obstacles's `avoidance_layers` bitmask.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'
    assert isinstance(layers, (int, float)), 'layers must be int or float'









    self._ptr.call_with_return(172,tuple([obstacle._ptr, layers]))

  @functools.native_method
  def obstacle_get_avoidance_layers(self, obstacle:'RID'   ) -> typing.Union[int]:
    r'''
				Returns the `avoidance_layers` bitmask of the specified `obstacle`.
			'''

    assert(not obstacle is None)
    assert isinstance(obstacle, RID), 'obstacle must be RID'








    _ret = 0
    _ret = self._ptr.call_with_return(173,tuple([obstacle._ptr]))
    return _ret


  @functools.native_method
  def parse_source_geometry_data(self, navigation_polygon:'py4godot_navigationpolygon.NavigationPolygon'   , source_geometry_data:'py4godot_navigationmeshsourcegeometrydata2d.NavigationMeshSourceGeometryData2D'   , root_node:'py4godot_node.Node'   , callback:'Callable' = None  ) -> None:
    r'''
				Parses the `SceneTree` for source geometry according to the properties of `navigation_polygon`. Updates the provided `source_geometry_data` resource with the resulting data. The resource can then be used to bake a navigation mesh with `bake_from_source_geometry_data`. After the process is finished the optional `callback` will be called.
				**Note:** This function needs to run on the main thread or with a deferred call as the SceneTree is not thread-safe.
				**Performance:** While convenient, reading data arrays from `Mesh` resources can affect the frame rate negatively. The data needs to be received from the GPU, stalling the `RenderingServer` in the process. For performance prefer the use of e.g. collision shapes or creating the data arrays entirely in code.
			'''
    if callback is None:
      callback = Callable.new0()

    assert(not navigation_polygon is None)
    assert isinstance(navigation_polygon, get_class('NavigationPolygon')), 'navigation_polygon must be NavigationPolygon'
    assert(not source_geometry_data is None)
    assert isinstance(source_geometry_data, get_class('NavigationMeshSourceGeometryData2D')), 'source_geometry_data must be NavigationMeshSourceGeometryData2D'
    assert(not root_node is None)
    assert isinstance(root_node, get_class('Node')), 'root_node must be Node'


    navigation_polygon.reference()
    source_geometry_data.reference()









    self._ptr.call_with_return(174,tuple([navigation_polygon._ptr, source_geometry_data._ptr, root_node._ptr, callback._ptr]))

  @functools.native_method
  def bake_from_source_geometry_data(self, navigation_polygon:'py4godot_navigationpolygon.NavigationPolygon'   , source_geometry_data:'py4godot_navigationmeshsourcegeometrydata2d.NavigationMeshSourceGeometryData2D'   , callback:'Callable' = None  ) -> None:
    r'''
				Bakes the provided `navigation_polygon` with the data from the provided `source_geometry_data`. After the process is finished the optional `callback` will be called.
			'''
    if callback is None:
      callback = Callable.new0()

    assert(not navigation_polygon is None)
    assert isinstance(navigation_polygon, get_class('NavigationPolygon')), 'navigation_polygon must be NavigationPolygon'
    assert(not source_geometry_data is None)
    assert isinstance(source_geometry_data, get_class('NavigationMeshSourceGeometryData2D')), 'source_geometry_data must be NavigationMeshSourceGeometryData2D'


    navigation_polygon.reference()
    source_geometry_data.reference()








    self._ptr.call_with_return(175,tuple([navigation_polygon._ptr, source_geometry_data._ptr, callback._ptr]))

  @functools.native_method
  def bake_from_source_geometry_data_async(self, navigation_polygon:'py4godot_navigationpolygon.NavigationPolygon'   , source_geometry_data:'py4godot_navigationmeshsourcegeometrydata2d.NavigationMeshSourceGeometryData2D'   , callback:'Callable' = None  ) -> None:
    r'''
				Bakes the provided `navigation_polygon` with the data from the provided `source_geometry_data` as an async task running on a background thread. After the process is finished the optional `callback` will be called.
			'''
    if callback is None:
      callback = Callable.new0()

    assert(not navigation_polygon is None)
    assert isinstance(navigation_polygon, get_class('NavigationPolygon')), 'navigation_polygon must be NavigationPolygon'
    assert(not source_geometry_data is None)
    assert isinstance(source_geometry_data, get_class('NavigationMeshSourceGeometryData2D')), 'source_geometry_data must be NavigationMeshSourceGeometryData2D'


    navigation_polygon.reference()
    source_geometry_data.reference()








    self._ptr.call_with_return(176,tuple([navigation_polygon._ptr, source_geometry_data._ptr, callback._ptr]))

  @functools.native_method
  def is_baking_navigation_polygon(self, navigation_polygon:'py4godot_navigationpolygon.NavigationPolygon'   ) -> typing.Union[bool]:
    r'''
				Returns `true` when the provided navigation polygon is being baked on a background thread.
			'''

    assert(not navigation_polygon is None)
    assert isinstance(navigation_polygon, get_class('NavigationPolygon')), 'navigation_polygon must be NavigationPolygon'


    navigation_polygon.reference()






    _ret = 0
    _ret = self._ptr.call_with_return(177,tuple([navigation_polygon._ptr]))
    return _ret


  @functools.native_method
  def source_geometry_parser_create(self) -> typing.Union['RID']:
    r'''
				Creates a new source geometry parser. If a `Callable` is set for the parser with `source_geometry_parser_set_callback` the callback will be called for every single node that gets parsed whenever `parse_source_geometry_data` is used.
			'''




    _ret = RID.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(178,tuple([]))
    return _ret


  @functools.native_method
  def source_geometry_parser_set_callback(self, parser:'RID'   , callback:'Callable'   ) -> None:
    r'''
				Sets the `callback` `Callable` for the specific source geometry `parser`. The `Callable` will receive a call with the following parameters:
				- `navigation_mesh` - The `NavigationPolygon` reference used to define the parse settings. Do NOT edit or add directly to the navigation mesh.
				- `source_geometry_data` - The `NavigationMeshSourceGeometryData2D` reference. Add custom source geometry for navigation mesh baking to this object.
				- `node` - The `Node` that is parsed.
			'''

    assert(not parser is None)
    assert isinstance(parser, RID), 'parser must be RID'
    assert(not callback is None)
    assert isinstance(callback, Callable), 'callback must be Callable'









    self._ptr.call_with_return(179,tuple([parser._ptr, callback._ptr]))

  @functools.native_method
  def simplify_path(self, path:'PackedVector2Array'   , epsilon:'float'   ) -> typing.Union['PackedVector2Array']:
    r'''
				Returns a simplified version of `path` with less critical path points removed. The simplification amount is in worlds units and controlled by `epsilon`. The simplification uses a variant of Ramer-Douglas-Peucker algorithm for curve point decimation.
				Path simplification can be helpful to mitigate various path following issues that can arise with certain agent types and script behaviors. E.g. "steering" agents or avoidance in "open fields".
			'''

    assert(not path is None)
    assert isinstance(path, PackedVector2Array), 'path must be PackedVector2Array'
    assert isinstance(epsilon, (int, float)), 'epsilon must be int or float'









    _ret = PackedVector2Array.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(180,tuple([path._ptr, epsilon]))
    return _ret


  @functools.native_method
  def free_rid(self, rid:'RID'   ) -> None:
    r'''
				Destroys the given RID.
			'''

    assert(not rid is None)
    assert isinstance(rid, RID), 'rid must be RID'








    self._ptr.call_with_return(181,tuple([rid._ptr]))

  @functools.native_method
  def set_active(self, active:'bool'   ) -> None:
    r'''
				Control activation of this server.
			'''

    assert isinstance(active, bool), 'active must be bool'








    self._ptr.call_with_return(182,tuple([active]))

  @functools.native_method
  def set_debug_enabled(self, enabled:'bool'   ) -> None:
    r'''
				If `true` enables debug mode on the NavigationServer.
			'''

    assert isinstance(enabled, bool), 'enabled must be bool'








    self._ptr.call_with_return(183,tuple([enabled]))

  @functools.native_method
  def get_debug_enabled(self) -> typing.Union[bool]:
    r'''
				Returns `true` when the NavigationServer has debug enabled.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(184,tuple([]))
    return _ret


  @functools.native_method
  def get_process_info(self, process_info:'int'  ) -> typing.Union[int]:
    r'''
				Returns information about the current state of the NavigationServer.
			'''

    assert isinstance(process_info, (int, float)), 'process_info must be int or float'








    _ret = 0
    _ret = self._ptr.call_with_return(185,tuple([process_info]))
    return _ret



register_cast_function('NavigationServer2D', NavigationServer2D.cast)
register_class('NavigationServer2D', NavigationServer2D)
