# distutils: language=c++
import py4godot.utils.functools as  functools
import py4godot.utils.utils as utils
from py4godot.classes.core import *
from py4godot.utils.smart_cast import smart_cast, register_cast_function, register_class, get_class
from py4godot.utils.CPPWrapper import CPPWrapper, constructor, static_method
import py4godot.utils.utils as c_utils
from py4godot.utils.CoreType import CoreType
import typing
import py4godot.variant_types
import py4godot.classes as classes
import py4godot.classes as classes
import py4godot.signals as signals

import typing
if typing.TYPE_CHECKING:
  pass
import py4godot.classes.Object as py4godot_object 
from py4godot.wrappers.wrappers import CPPTimeWrapper

_Time_singleton_instance = None
class Month:
  MONTH_JANUARY = 1
  MONTH_FEBRUARY = 2
  MONTH_MARCH = 3
  MONTH_APRIL = 4
  MONTH_MAY = 5
  MONTH_JUNE = 6
  MONTH_JULY = 7
  MONTH_AUGUST = 8
  MONTH_SEPTEMBER = 9
  MONTH_OCTOBER = 10
  MONTH_NOVEMBER = 11
  MONTH_DECEMBER = 12
class Weekday:
  WEEKDAY_SUNDAY = 0
  WEEKDAY_MONDAY = 1
  WEEKDAY_TUESDAY = 2
  WEEKDAY_WEDNESDAY = 3
  WEEKDAY_THURSDAY = 4
  WEEKDAY_FRIDAY = 5
  WEEKDAY_SATURDAY = 6


class Time(py4godot_object.Object):
  r'''
		The Time singleton allows converting time between various formats and also getting time information from the system.
		This class conforms with as many of the ISO 8601 standards as possible. All dates follow the Proleptic Gregorian calendar. As such, the day before `1582-10-15` is `1582-10-14`, not `1582-10-04`. The year before 1 AD (aka 1 BC) is number `0`, with the year before that (2 BC) being `-1`, etc.
		Conversion methods assume "the same timezone", and do not handle timezone conversions or DST automatically. Leap seconds are also not handled, they must be done manually if desired. Suffixes such as "Z" are not handled, you need to strip them away manually.
		When getting time information from the system, the time can either be in the local timezone or UTC depending on the `utc` parameter. However, the `get_unix_time_from_system` method always uses UTC as it returns the seconds passed since the `url=https://en.wikipedia.org/wiki/Unix_time`Unix epoch`/url`.
		**Important:** The `_from_system` methods use the system clock that the user can manually set. **Never use** this method for precise time calculation since its results are subject to automatic adjustments by the user or the operating system. **Always use** `get_ticks_usec` or `get_ticks_msec` for precise time calculation instead, since they are guaranteed to be monotonic (i.e. never decrease).
	'''


  def generate_wrapper(self):
    return CPPTimeWrapper()
  def init_signals(self):
    super().init_signals()

  @staticmethod
  def construct_without_init():
    cls = Time.__new__(Time)
    cls.shouldBeDeleted = True
    cls.casted_from = None
    cls.init_signals()
    return cls
  @staticmethod
  def get_type():
    return 'Time'

  @staticmethod
  def cast(other:'Object'):
    assert other != None # Object to be casted must not be None
    cls = Time.construct_without_init()
    cls._ptr = CPPTimeWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls
  @staticmethod
  def cast_without_reference(other: 'Object'):
    assert other != None # Object to be casted must not be None
    cls = Time.construct_without_init()
    cls._ptr = CPPTimeWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls
  @staticmethod
  def instance():
    global _Time_singleton_instance
    if _Time_singleton_instance is None:
      singleton = Time()
      singleton._ptr = constructor(842,0, ())
      _Time_singleton_instance = singleton
    return _Time_singleton_instance

  @functools.native_method
  def get_datetime_dict_from_unix_time(self, unix_time_val:'int'   ) -> typing.Union['Dictionary']:
    r'''
				Converts the given Unix timestamp to a dictionary of keys: `year`, `month`, `day`, `weekday`, `hour`, `minute`, and `second`.
				The returned Dictionary's values will be the same as the `get_datetime_dict_from_system` if the Unix timestamp is the current time, with the exception of Daylight Savings Time as it cannot be determined from the epoch.
			'''

    assert isinstance(unix_time_val, (int, float)), 'unix_time_val must be int or float'








    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(49,tuple([unix_time_val]))
    return _ret


  @functools.native_method
  def get_date_dict_from_unix_time(self, unix_time_val:'int'   ) -> typing.Union['Dictionary']:
    r'''
				Converts the given Unix timestamp to a dictionary of keys: `year`, `month`, `day`, and `weekday`.
			'''

    assert isinstance(unix_time_val, (int, float)), 'unix_time_val must be int or float'








    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(50,tuple([unix_time_val]))
    return _ret


  @functools.native_method
  def get_time_dict_from_unix_time(self, unix_time_val:'int'   ) -> typing.Union['Dictionary']:
    r'''
				Converts the given time to a dictionary of keys: `hour`, `minute`, and `second`.
			'''

    assert isinstance(unix_time_val, (int, float)), 'unix_time_val must be int or float'








    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(51,tuple([unix_time_val]))
    return _ret


  @functools.native_method
  def get_datetime_string_from_unix_time(self, unix_time_val:'int'   , use_space:'bool' =False  ) -> typing.Union[str]:
    r'''
				Converts the given Unix timestamp to an ISO 8601 date and time string (YYYY-MM-DDTHH:MM:SS).
				If `use_space` is `true`, the date and time bits are separated by an empty space character instead of the letter T.
			'''

    assert isinstance(unix_time_val, (int, float)), 'unix_time_val must be int or float'
    assert isinstance(use_space, bool), 'use_space must be bool'









    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(52,tuple([unix_time_val, use_space]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_date_string_from_unix_time(self, unix_time_val:'int'   ) -> typing.Union[str]:
    r'''
				Converts the given Unix timestamp to an ISO 8601 date string (YYYY-MM-DD).
			'''

    assert isinstance(unix_time_val, (int, float)), 'unix_time_val must be int or float'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(53,tuple([unix_time_val]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_time_string_from_unix_time(self, unix_time_val:'int'   ) -> typing.Union[str]:
    r'''
				Converts the given Unix timestamp to an ISO 8601 time string (HH:MM:SS).
			'''

    assert isinstance(unix_time_val, (int, float)), 'unix_time_val must be int or float'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(54,tuple([unix_time_val]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_datetime_dict_from_datetime_string(self, datetime:'str'   , weekday:'bool'   ) -> typing.Union['Dictionary']:
    r'''
				Converts the given ISO 8601 date and time string (YYYY-MM-DDTHH:MM:SS) to a dictionary of keys: `year`, `month`, `day`, `code skip-lint`weekday`, `hour`, `minute`, and `second`.
				If `weekday` is `false`, then the `code skip-lint`weekday` entry is excluded (the calculation is relatively expensive).
				**Note:** Any decimal fraction in the time string will be ignored silently.
			'''

    assert(not datetime is None)
    assert isinstance(datetime, (str, String)), 'datetime must be str or String'
    assert isinstance(weekday, bool), 'weekday must be bool'






    assert(isinstance(datetime, (str, String)))
    py_string_datetime = datetime if isinstance(datetime, StringName) else c_utils.py_string_to_string(datetime)


    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(55,tuple([py_string_datetime._ptr, weekday]))
    return _ret


  @functools.native_method
  def get_datetime_string_from_datetime_dict(self, datetime:'Dictionary'   , use_space:'bool'   ) -> typing.Union[str]:
    r'''
				Converts the given dictionary of keys to an ISO 8601 date and time string (YYYY-MM-DDTHH:MM:SS).
				The given dictionary can be populated with the following keys: `year`, `month`, `day`, `hour`, `minute`, and `second`. Any other entries (including `dst`) are ignored.
				If the dictionary is empty, `0` is returned. If some keys are omitted, they default to the equivalent values for the Unix epoch timestamp 0 (1970-01-01 at 00:00:00).
				If `use_space` is `true`, the date and time bits are separated by an empty space character instead of the letter T.
			'''

    assert(not datetime is None)
    assert isinstance(datetime, Dictionary), 'datetime must be Dictionary'
    assert isinstance(use_space, bool), 'use_space must be bool'









    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(56,tuple([datetime._ptr, use_space]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_unix_time_from_datetime_dict(self, datetime:'Dictionary'   ) -> typing.Union[int]:
    r'''
				Converts a dictionary of time values to a Unix timestamp.
				The given dictionary can be populated with the following keys: `year`, `month`, `day`, `hour`, `minute`, and `second`. Any other entries (including `dst`) are ignored.
				If the dictionary is empty, `0` is returned. If some keys are omitted, they default to the equivalent values for the Unix epoch timestamp 0 (1970-01-01 at 00:00:00).
				You can pass the output from `get_datetime_dict_from_unix_time` directly into this function and get the same as what was put in.
				**Note:** Unix timestamps are often in UTC. This method does not do any timezone conversion, so the timestamp will be in the same timezone as the given datetime dictionary.
			'''

    assert(not datetime is None)
    assert isinstance(datetime, Dictionary), 'datetime must be Dictionary'








    _ret = 0
    _ret = self._ptr.call_with_return(57,tuple([datetime._ptr]))
    return _ret


  @functools.native_method
  def get_unix_time_from_datetime_string(self, datetime:'str'   ) -> typing.Union[int]:
    r'''
				Converts the given ISO 8601 date and/or time string to a Unix timestamp. The string can contain a date only, a time only, or both.
				**Note:** Unix timestamps are often in UTC. This method does not do any timezone conversion, so the timestamp will be in the same timezone as the given datetime string.
				**Note:** Any decimal fraction in the time string will be ignored silently.
			'''

    assert(not datetime is None)
    assert isinstance(datetime, (str, String)), 'datetime must be str or String'






    assert(isinstance(datetime, (str, String)))
    py_string_datetime = datetime if isinstance(datetime, StringName) else c_utils.py_string_to_string(datetime)

    _ret = 0
    _ret = self._ptr.call_with_return(58,tuple([py_string_datetime._ptr]))
    return _ret


  @functools.native_method
  def get_offset_string_from_offset_minutes(self, offset_minutes:'int'   ) -> typing.Union[str]:
    r'''
				Converts the given timezone offset in minutes to a timezone offset string. For example, -480 returns "-08:00", 345 returns "+05:45", and 0 returns "+00:00".
			'''

    assert isinstance(offset_minutes, (int, float)), 'offset_minutes must be int or float'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(59,tuple([offset_minutes]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_datetime_dict_from_system(self, utc:'bool' =False  ) -> typing.Union['Dictionary']:
    r'''
				Returns the current date as a dictionary of keys: `year`, `month`, `day`, `weekday`, `hour`, `minute`, `second`, and `dst` (Daylight Savings Time).
			'''

    assert isinstance(utc, bool), 'utc must be bool'








    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(60,tuple([utc]))
    return _ret


  @functools.native_method
  def get_date_dict_from_system(self, utc:'bool' =False  ) -> typing.Union['Dictionary']:
    r'''
				Returns the current date as a dictionary of keys: `year`, `month`, `day`, and `weekday`.
				The returned values are in the system's local time when `utc` is `false`, otherwise they are in UTC.
			'''

    assert isinstance(utc, bool), 'utc must be bool'








    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(61,tuple([utc]))
    return _ret


  @functools.native_method
  def get_time_dict_from_system(self, utc:'bool' =False  ) -> typing.Union['Dictionary']:
    r'''
				Returns the current time as a dictionary of keys: `hour`, `minute`, and `second`.
				The returned values are in the system's local time when `utc` is `false`, otherwise they are in UTC.
			'''

    assert isinstance(utc, bool), 'utc must be bool'








    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(62,tuple([utc]))
    return _ret


  @functools.native_method
  def get_datetime_string_from_system(self, utc:'bool' =False  , use_space:'bool' =False  ) -> typing.Union[str]:
    r'''
				Returns the current date and time as an ISO 8601 date and time string (YYYY-MM-DDTHH:MM:SS).
				The returned values are in the system's local time when `utc` is `false`, otherwise they are in UTC.
				If `use_space` is `true`, the date and time bits are separated by an empty space character instead of the letter T.
			'''

    assert isinstance(utc, bool), 'utc must be bool'
    assert isinstance(use_space, bool), 'use_space must be bool'









    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(63,tuple([utc, use_space]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_date_string_from_system(self, utc:'bool' =False  ) -> typing.Union[str]:
    r'''
				Returns the current date as an ISO 8601 date string (YYYY-MM-DD).
				The returned values are in the system's local time when `utc` is `false`, otherwise they are in UTC.
			'''

    assert isinstance(utc, bool), 'utc must be bool'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(64,tuple([utc]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_time_string_from_system(self, utc:'bool' =False  ) -> typing.Union[str]:
    r'''
				Returns the current time as an ISO 8601 time string (HH:MM:SS).
				The returned values are in the system's local time when `utc` is `false`, otherwise they are in UTC.
			'''

    assert isinstance(utc, bool), 'utc must be bool'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(65,tuple([utc]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_time_zone_from_system(self) -> typing.Union['Dictionary']:
    r'''
				Returns the current time zone as a dictionary of keys: `bias` and `name`.
				- `bias` is the offset from UTC in minutes, since not all time zones are multiples of an hour from UTC.
				- `name` is the localized name of the time zone, according to the OS locale settings of the current user.
			'''




    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(66,tuple([]))
    return _ret


  @functools.native_method
  def get_unix_time_from_system(self) -> typing.Union[float]:
    r'''
				Returns the current Unix timestamp in seconds based on the system time in UTC. This method is implemented by the operating system and always returns the time in UTC. The Unix timestamp is the number of seconds passed since 1970-01-01 at 00:00:00, the `url=https://en.wikipedia.org/wiki/Unix_time`Unix epoch`/url`.
				**Note:** Unlike other methods that use integer timestamps, this method returns the timestamp as a `float` for sub-second precision.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(67,tuple([]))
    return _ret


  @functools.native_method
  def get_ticks_msec(self) -> typing.Union[int]:
    r'''
				Returns the amount of time passed in milliseconds since the engine started.
				Will always be positive or 0 and uses a 64-bit value (it will wrap after roughly 500 million years).
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(68,tuple([]))
    return _ret


  @functools.native_method
  def get_ticks_usec(self) -> typing.Union[int]:
    r'''
				Returns the amount of time passed in microseconds since the engine started.
				Will always be positive or 0 and uses a 64-bit value (it will wrap after roughly half a million years).
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(69,tuple([]))
    return _ret



register_cast_function('Time', Time.cast)
register_class('Time', Time)
