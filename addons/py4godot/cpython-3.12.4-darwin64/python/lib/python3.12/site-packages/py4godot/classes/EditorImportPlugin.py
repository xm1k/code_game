# distutils: language=c++
import py4godot.utils.functools as  functools
import py4godot.utils.utils as utils
from py4godot.classes.core import *
from py4godot.utils.smart_cast import smart_cast, register_cast_function, register_class, get_class
from py4godot.utils.CPPWrapper import CPPWrapper, constructor, static_method
import py4godot.utils.utils as c_utils
from py4godot.utils.CoreType import CoreType
import typing
import py4godot.variant_types
import py4godot.classes as classes
import py4godot.classes as classes
import py4godot.signals as signals

import typing
if typing.TYPE_CHECKING:
  pass
  import py4godot.classes.StringTypedArray as py4godot_stringtypedarray 
  import py4godot.classes.DictionaryTypedArray as py4godot_dictionarytypedarray 
import py4godot.classes.ResourceImporter as py4godot_resourceimporter 
import py4godot.classes.StringTypedArray as py4godot_stringtypedarray 
import py4godot.classes.DictionaryTypedArray as py4godot_dictionarytypedarray 
from py4godot.wrappers.wrappers import CPPEditorImportPluginWrapper


class EditorImportPlugin(py4godot_resourceimporter.ResourceImporter):
  r'''
		`EditorImportPlugin`s provide a way to extend the editor's resource import functionality. Use them to import resources from custom files or to provide alternatives to the editor's existing importers.
		EditorImportPlugins work by associating with specific file extensions and a resource type. See `_get_recognized_extensions` and `_get_resource_type`. They may optionally specify some import presets that affect the import process. EditorImportPlugins are responsible for creating the resources and saving them in the `.godot/imported` directory (see `ProjectSettings.application/config/use_hidden_project_data_directory`).
		Below is an example EditorImportPlugin that imports a `Mesh` from a file with the extension ".special" or ".spec":
		
		```gdscript
		@tool
		extends EditorImportPlugin

		func _get_importer_name():
			return "my.special.plugin"

		func _get_visible_name():
			return "Special Mesh"

		func _get_recognized_extensions():
			return `"special", "spec"`

		func _get_save_extension():
			return "mesh"

		func _get_resource_type():
			return "Mesh"

		func _get_preset_count():
			return 1

		func _get_preset_name(preset_index):
			return "Default"

		func _get_import_options(path, preset_index):
			return `{"name": "my_option", "default_value": false}`

		func _import(source_file, save_path, options, platform_variants, gen_files):
			var file = FileAccess.open(source_file, FileAccess.READ)
			if file == null:
				return FAILED
			var mesh = ArrayMesh.new()
			# Fill the Mesh with data read in "file", left as an exercise to the reader.

			var filename = save_path + "." + _get_save_extension()
			return ResourceSaver.save(mesh, filename)
		```
		
		
		To use `EditorImportPlugin`, register it using the `EditorPlugin.add_import_plugin` method first.
	'''

  @staticmethod
  def constructor():
    class_ = EditorImportPlugin.construct_without_init()
    class_._ptr = constructor(288,0, ())
    return class_
  @staticmethod
  def new():
    class_ = EditorImportPlugin.construct_without_init()
    class_._ptr = constructor(288,0, ())
    return class_
  def __init__(self):
    self._ptr =  CPPEditorImportPluginWrapper()
    self.casted_from = None
    if c_utils.shouldCreateObject:
      self._ptr = constructor(288,0, ())


  def generate_wrapper(self):
    return CPPEditorImportPluginWrapper()
  def init_signals(self):
    super().init_signals()

  @staticmethod
  def construct_without_init():
    cls = EditorImportPlugin.__new__(EditorImportPlugin)
    cls.shouldBeDeleted = True
    cls.casted_from = None
    cls.init_signals()
    return cls
  @staticmethod
  def get_type():
    return 'EditorImportPlugin'

  def __del__(self):
    if self.casted_from is None:
      self._ptr.call_with_return(69, ())
      self._ptr = None
  @staticmethod
  def cast(other:'Object'):
    assert other != None # Object to be casted must not be None
    cls = EditorImportPlugin.construct_without_init()
    cls._ptr = CPPEditorImportPluginWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    cls.casted_from = other
    return cls
  @staticmethod
  def cast_without_reference(other: 'Object'):
    assert other != None # Object to be casted must not be None
    cls = EditorImportPlugin.construct_without_init()
    cls._ptr = CPPEditorImportPluginWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls

  @functools.native_method
  def _get_importer_name(self) -> typing.Union[str]:
    r'''
				Gets the unique name of the importer.
			'''




    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(54,tuple([]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def _get_visible_name(self) -> typing.Union[str]:
    r'''
				Gets the name to display in the import window. You should choose this name as a continuation to "Import as", e.g. "Import as Special Mesh".
			'''




    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(55,tuple([]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def _get_preset_count(self) -> typing.Union[int]:
    r'''
				Gets the number of initial presets defined by the plugin. Use `_get_import_options` to get the default options for the preset and `_get_preset_name` to get the name of the preset.
				By default, there are no presets.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(56,tuple([]))
    return _ret


  @functools.native_method
  def _get_preset_name(self, preset_index:'int'   ) -> typing.Union[str]:
    r'''
				Gets the name of the options preset at this index.
			'''

    assert isinstance(preset_index, (int, float)), 'preset_index must be int or float'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(57,tuple([preset_index]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def _get_recognized_extensions(self) -> typing.Union['PackedStringArray']:
    r'''
				Gets the list of file extensions to associate with this loader (case-insensitive). e.g. ``"obj"``.
			'''




    _ret = PackedStringArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(58,tuple([]))
    return _ret


  @functools.native_method
  def _get_import_options(self, path:'str'   , preset_index:'int'   ) -> typing.Union['py4godot_dictionarytypedarray.DictionaryTypedArray']:
    r'''
				Gets the options and default values for the preset at this index. Returns an Array of Dictionaries with the following keys: `name`, `default_value`, `property_hint` (optional), `hint_string` (optional), `usage` (optional).
			'''

    assert(not path is None)
    assert isinstance(path, (str, String)), 'path must be str or String'
    assert isinstance(preset_index, (int, float)), 'preset_index must be int or float'






    assert(isinstance(path, (str, String)))
    py_string_path = path if isinstance(path, StringName) else c_utils.py_string_to_string(path)


    _ret = py4godot_dictionarytypedarray.DictionaryTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(59,tuple([py_string_path._ptr, preset_index]))
    return _ret


  @functools.native_method
  def _get_save_extension(self) -> typing.Union[str]:
    r'''
				Gets the extension used to save this resource in the `.godot/imported` directory (see `ProjectSettings.application/config/use_hidden_project_data_directory`).
			'''




    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(60,tuple([]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def _get_resource_type(self) -> typing.Union[str]:
    r'''
				Gets the Godot resource type associated with this loader. e.g. `"Mesh"` or `"Animation"`.
			'''




    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(61,tuple([]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def _get_priority(self) -> typing.Union[float]:
    r'''
				Gets the priority of this plugin for the recognized extension. Higher priority plugins will be preferred. The default priority is `1.0`.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(62,tuple([]))
    return _ret


  @functools.native_method
  def _get_import_order(self) -> typing.Union[int]:
    r'''
				Gets the order of this importer to be run when importing resources. Importers with _lower_ import orders will be called first, and higher values will be called later. Use this to ensure the importer runs after the dependencies are already imported. The default import order is `0` unless overridden by a specific importer. See `enum ResourceImporter.ImportOrder` for some predefined values.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(63,tuple([]))
    return _ret


  @functools.native_method
  def _get_format_version(self) -> typing.Union[int]:
    r'''
				Gets the format version of this importer. Increment this version when making incompatible changes to the format of the imported resources.
				If not overridden, the format version is `0`.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(64,tuple([]))
    return _ret


  @functools.native_method
  def _get_option_visibility(self, path:'str'   , option_name:'object'   , options:'Dictionary'   ) -> typing.Union[bool]:
    r'''
				Gets whether the import option specified by `option_name` should be visible in the Import dock. The default implementation always returns `true`, making all options visible. This is mainly useful for hiding options that depend on others if one of them is disabled.
				
				```gdscript
				func _get_option_visibility(path, option_name, options):
					# Only show the lossy quality setting if the compression mode is set to "Lossy".
					if option_name == "compress/lossy_quality" and options.has("compress/mode"):
						return int(options`"compress/mode"`) == COMPRESS_LOSSY # This is a constant that you set

					return true
				```
				
				
			'''

    assert(not path is None)
    assert isinstance(path, (str, String)), 'path must be str or String'
    assert(not option_name is None)
    assert isinstance(option_name, (str, StringName)), 'option_name must be str or StringName'
    assert(not options is None)
    assert isinstance(options, Dictionary), 'options must be Dictionary'






    assert(isinstance(path, (str, String)))
    py_string_path = path if isinstance(path, StringName) else c_utils.py_string_to_string(path)
    assert(isinstance(option_name, (str, StringName)))
    py_stringname_option_name = option_name if isinstance(option_name, StringName) else c_utils.py_string_to_string_name(option_name)


    _ret = 0
    _ret = self._ptr.call_with_return(65,tuple([py_string_path._ptr, py_stringname_option_name._ptr, options._ptr]))
    return _ret


  @functools.native_method
  def _import(self, source_file:'str'   , save_path:'str'   , options:'Dictionary'   , platform_variants:'py4godot_stringtypedarray.StringTypedArray'   , gen_files:'py4godot_stringtypedarray.StringTypedArray'   ) -> typing.Union[int]:
    r'''
				Imports `source_file` with the import `options` specified. Should return `constant @GlobalScope.OK` if the import is successful, other values indicate failure.
				The imported resource is expected to be saved to `save_path + "." + _get_save_extension()`. If a different variant is preferred for a `url=$DOCS_URL/tutorials/export/feature_tags.html`feature tag`/url`, save the variant to `save_path + "." + tag + "." + _get_save_extension()` and add the feature tag to `platform_variants`.
				If additional resource files are generated in the resource filesystem (`res://`), add their full path to `gen_files` so that the editor knows they depend on `source_file`.
				This method must be overridden to do the actual importing work. See this class' description for an example of overriding this method.
			'''

    assert(not source_file is None)
    assert isinstance(source_file, (str, String)), 'source_file must be str or String'
    assert(not save_path is None)
    assert isinstance(save_path, (str, String)), 'save_path must be str or String'
    assert(not options is None)
    assert isinstance(options, Dictionary), 'options must be Dictionary'
    assert isinstance(platform_variants, get_class('StringTypedArray')), 'platform_variants must be StringTypedArray'
    assert isinstance(gen_files, get_class('StringTypedArray')), 'gen_files must be StringTypedArray'






    assert(isinstance(source_file, (str, String)))
    py_string_source_file = source_file if isinstance(source_file, StringName) else c_utils.py_string_to_string(source_file)
    assert(isinstance(save_path, (str, String)))
    py_string_save_path = save_path if isinstance(save_path, StringName) else c_utils.py_string_to_string(save_path)




    _ret:int
    _ret = self._ptr.call_with_return(66,tuple([py_string_source_file._ptr, py_string_save_path._ptr, options._ptr, platform_variants._ptr, gen_files._ptr]))
    return _ret


  @functools.native_method
  def _can_import_threaded(self) -> typing.Union[bool]:
    r'''
				Tells whether this importer can be run in parallel on threads, or, on the contrary, it's only safe for the editor to call it from the main thread, for one file at a time.
				If this importer's implementation is thread-safe and can be run in parallel, override this with `true` to optimize for concurrency.
				If not overridden, returns `false`.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(67,tuple([]))
    return _ret


  @functools.native_method
  def append_import_external_resource(self, path:'str'   , custom_options:'Dictionary' = None  , custom_importer:'str' =""  , generator_parameters:'object' =None  ) -> typing.Union[int]:
    r'''
				This function can only be called during the `_import` callback and it allows manually importing resources from it. This is useful when the imported file generates external resources that require importing (as example, images). Custom parameters for the ".import" file can be passed via the `custom_options`. Additionally, in cases where multiple importers can handle a file, the `custom_importer` can be specified to force a specific one. This function performs a resource import and returns immediately with a success or error code. `generator_parameters` defines optional extra metadata which will be stored as `code skip-lint`generator_parameters` in the `remap` section of the `.import` file, for example to store a md5 hash of the source data.
			'''
    if custom_options is None:
      custom_options = Dictionary.new0()
    if custom_importer is None:
      custom_importer = String.new0()


    assert(not path is None)
    assert isinstance(path, (str, String)), 'path must be str or String'
    

    if not (type(generator_parameters).__name__ in py4godot.variant_types.core_types or isinstance(generator_parameters, classes.Object.Object)):
      raise ValueError(
                f"Unsupported type for argument 'generator_parameters': "
                f"{type(generator_parameters).__name__}. "
                "Supported types are: Godot types (Vector3, Vector2, Array, Object, ...) "
                "and built-in types int, float, and bool."
            )





    assert(isinstance(path, (str, String)))
    py_string_path = path if isinstance(path, StringName) else c_utils.py_string_to_string(path)

    assert(isinstance(custom_importer, (str, String)))
    py_string_custom_importer = custom_importer if isinstance(custom_importer, StringName) else c_utils.py_string_to_string(custom_importer)


    _ret:int
    _ret = self._ptr.call_with_return(68,tuple([py_string_path._ptr, custom_options._ptr, py_string_custom_importer._ptr, generator_parameters]))
    return _ret



register_cast_function('EditorImportPlugin', EditorImportPlugin.cast)
register_class('EditorImportPlugin', EditorImportPlugin)
