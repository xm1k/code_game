# distutils: language=c++
import py4godot.utils.functools as  functools
import py4godot.utils.utils as utils
from py4godot.classes.core import *
from py4godot.utils.smart_cast import smart_cast, register_cast_function, register_class, get_class
from py4godot.utils.CPPWrapper import CPPWrapper, constructor, static_method
import py4godot.utils.utils as c_utils
from py4godot.utils.CoreType import CoreType
import typing
import py4godot.variant_types
import py4godot.classes as classes
import py4godot.classes as classes
import py4godot.signals as signals

import typing
if typing.TYPE_CHECKING:
  pass
  import py4godot.classes.Resource as py4godot_resource 
  import py4godot.classes.InputEvent as py4godot_inputevent 
  import py4godot.classes.intTypedArray as py4godot_inttypedarray 
import py4godot.classes.Object as py4godot_object 
import py4godot.classes.intTypedArray as py4godot_inttypedarray 
from py4godot.wrappers.wrappers import CPPInputWrapper

_Input_singleton_instance = None
class MouseMode:
  MOUSE_MODE_VISIBLE = 0
  MOUSE_MODE_HIDDEN = 1
  MOUSE_MODE_CAPTURED = 2
  MOUSE_MODE_CONFINED = 3
  MOUSE_MODE_CONFINED_HIDDEN = 4
  MOUSE_MODE_MAX = 5
class CursorShape:
  CURSOR_ARROW = 0
  CURSOR_IBEAM = 1
  CURSOR_POINTING_HAND = 2
  CURSOR_CROSS = 3
  CURSOR_WAIT = 4
  CURSOR_BUSY = 5
  CURSOR_DRAG = 6
  CURSOR_CAN_DROP = 7
  CURSOR_FORBIDDEN = 8
  CURSOR_VSIZE = 9
  CURSOR_HSIZE = 10
  CURSOR_BDIAGSIZE = 11
  CURSOR_FDIAGSIZE = 12
  CURSOR_MOVE = 13
  CURSOR_VSPLIT = 14
  CURSOR_HSPLIT = 15
  CURSOR_HELP = 16


class Input(py4godot_object.Object):
  r'''
		The `Input` singleton handles key presses, mouse buttons and movement, gamepads, and input actions. Actions and their events can be set in the **Input Map** tab in **Project > Project Settings**, or with the `InputMap` class.
		**Note:** `Input`'s methods reflect the global input state and are not affected by `Control.accept_event` or `Viewport.set_input_as_handled`, as those methods only deal with the way input is propagated in the `SceneTree`.
	'''


  def generate_wrapper(self):
    return CPPInputWrapper()
  def init_signals(self):
    super().init_signals()
    joy_connection_changed_name = utils.py_string_to_string_name("joy_connection_changed")
    self.joy_connection_changed = signals.BuiltinSignal(self, joy_connection_changed_name)

  @staticmethod
  def construct_without_init():
    cls = Input.__new__(Input)
    cls.shouldBeDeleted = True
    cls.casted_from = None
    cls.init_signals()
    return cls
  @staticmethod
  def get_type():
    return 'Input'

  @staticmethod
  def cast(other:'Object'):
    assert other != None # Object to be casted must not be None
    cls = Input.construct_without_init()
    cls._ptr = CPPInputWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls
  @staticmethod
  def cast_without_reference(other: 'Object'):
    assert other != None # Object to be casted must not be None
    cls = Input.construct_without_init()
    cls._ptr = CPPInputWrapper()
    cls._ptr_before = other._ptr # Save pointer
    cls._ptr.copy_gdowner(cls._ptr_before)
    return cls
  @staticmethod
  def instance():
    global _Input_singleton_instance
    if _Input_singleton_instance is None:
      singleton = Input()
      singleton._ptr = constructor(412,0, ())
      _Input_singleton_instance = singleton
    return _Input_singleton_instance

  @property
  def mouse_mode(self) -> typing.Union[int]:
    r''''''
    _ret = self. get_mouse_mode()
    return _ret
  @mouse_mode.setter
  def mouse_mode(self,  value:'int'):
    self.set_mouse_mode(value)
  @property
  def use_accumulated_input(self) -> typing.Union[bool]:
    r''''''
    _ret = self. is_using_accumulated_input()
    return _ret
  @use_accumulated_input.setter
  def use_accumulated_input(self,  value:'bool'):
    self.set_use_accumulated_input(value)
  @property
  def emulate_mouse_from_touch(self) -> typing.Union[bool]:
    r''''''
    _ret = self. is_emulating_mouse_from_touch()
    return _ret
  @emulate_mouse_from_touch.setter
  def emulate_mouse_from_touch(self,  value:'bool'):
    self.set_emulate_mouse_from_touch(value)
  @property
  def emulate_touch_from_mouse(self) -> typing.Union[bool]:
    r''''''
    _ret = self. is_emulating_touch_from_mouse()
    return _ret
  @emulate_touch_from_mouse.setter
  def emulate_touch_from_mouse(self,  value:'bool'):
    self.set_emulate_touch_from_mouse(value)
  @functools.native_method
  def is_anything_pressed(self) -> typing.Union[bool]:
    r'''
				Returns `true` if any action, key, joypad button, or mouse button is being pressed. This will also return `true` if any action is simulated via code by calling `action_press`.
			'''




    _ret = 0
    _ret = self._ptr.call_with_return(49,tuple([]))
    return _ret


  @functools.native_method
  def is_key_pressed(self, keycode:'int'  ) -> typing.Union[bool]:
    r'''
				Returns `true` if you are pressing the Latin key in the current keyboard layout. You can pass a `enum Key` constant.
				`is_key_pressed` is only recommended over `is_physical_key_pressed` in non-game applications. This ensures that shortcut keys behave as expected depending on the user's keyboard layout, as keyboard shortcuts are generally dependent on the keyboard layout in non-game applications. If in doubt, use `is_physical_key_pressed`.
				**Note:** Due to keyboard ghosting, `is_key_pressed` may return `false` even if one of the action's keys is pressed. See `url=$DOCS_URL/tutorials/inputs/input_examples.html#keyboard-events`Input examples`/url` in the documentation for more information.
			'''

    assert isinstance(keycode, (int, float)), 'keycode must be int or float'








    _ret = 0
    _ret = self._ptr.call_with_return(50,tuple([keycode]))
    return _ret


  @functools.native_method
  def is_physical_key_pressed(self, keycode:'int'  ) -> typing.Union[bool]:
    r'''
				Returns `true` if you are pressing the key in the physical location on the 101/102-key US QWERTY keyboard. You can pass a `enum Key` constant.
				`is_physical_key_pressed` is recommended over `is_key_pressed` for in-game actions, as it will make `kbd`W`/kbd`/`kbd`A`/kbd`/`kbd`S`/kbd`/`kbd`D`/kbd` layouts work regardless of the user's keyboard layout. `is_physical_key_pressed` will also ensure that the top row number keys work on any keyboard layout. If in doubt, use `is_physical_key_pressed`.
				**Note:** Due to keyboard ghosting, `is_physical_key_pressed` may return `false` even if one of the action's keys is pressed. See `url=$DOCS_URL/tutorials/inputs/input_examples.html#keyboard-events`Input examples`/url` in the documentation for more information.
			'''

    assert isinstance(keycode, (int, float)), 'keycode must be int or float'








    _ret = 0
    _ret = self._ptr.call_with_return(51,tuple([keycode]))
    return _ret


  @functools.native_method
  def is_key_label_pressed(self, keycode:'int'  ) -> typing.Union[bool]:
    r'''
				Returns `true` if you are pressing the key with the `keycode` printed on it. You can pass a `enum Key` constant or any Unicode character code.
			'''

    assert isinstance(keycode, (int, float)), 'keycode must be int or float'








    _ret = 0
    _ret = self._ptr.call_with_return(52,tuple([keycode]))
    return _ret


  @functools.native_method
  def is_mouse_button_pressed(self, button:'int'  ) -> typing.Union[bool]:
    r'''
				Returns `true` if you are pressing the mouse button specified with `enum MouseButton`.
			'''

    assert isinstance(button, (int, float)), 'button must be int or float'








    _ret = 0
    _ret = self._ptr.call_with_return(53,tuple([button]))
    return _ret


  @functools.native_method
  def is_joy_button_pressed(self, device:'int'   , button:'int'  ) -> typing.Union[bool]:
    r'''
				Returns `true` if you are pressing the joypad button at index `button`.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'
    assert isinstance(button, (int, float)), 'button must be int or float'









    _ret = 0
    _ret = self._ptr.call_with_return(54,tuple([device, button]))
    return _ret


  @functools.native_method
  def is_action_pressed(self, action:'object'   , exact_match:'bool' =False  ) -> typing.Union[bool]:
    r'''
				Returns `true` if you are pressing the action event.
				If `exact_match` is `false`, it ignores additional input modifiers for `InputEventKey` and `InputEventMouseButton` events, and the direction for `InputEventJoypadMotion` events.
				**Note:** Due to keyboard ghosting, `is_action_pressed` may return `false` even if one of the action's keys is pressed. See `url=$DOCS_URL/tutorials/inputs/input_examples.html#keyboard-events`Input examples`/url` in the documentation for more information.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert isinstance(exact_match, bool), 'exact_match must be bool'






    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)


    _ret = 0
    _ret = self._ptr.call_with_return(55,tuple([py_stringname_action._ptr, exact_match]))
    return _ret


  @functools.native_method
  def is_action_just_pressed(self, action:'object'   , exact_match:'bool' =False  ) -> typing.Union[bool]:
    r'''
				Returns `true` when the user has _started_ pressing the action event in the current frame or physics tick. It will only return `true` on the frame or tick that the user pressed down the button.
				This is useful for code that needs to run only once when an action is pressed, instead of every frame while it's pressed.
				If `exact_match` is `false`, it ignores additional input modifiers for `InputEventKey` and `InputEventMouseButton` events, and the direction for `InputEventJoypadMotion` events.
				**Note:** Returning `true` does not imply that the action is _still_ pressed. An action can be pressed and released again rapidly, and `true` will still be returned so as not to miss input.
				**Note:** Due to keyboard ghosting, `is_action_just_pressed` may return `false` even if one of the action's keys is pressed. See `url=$DOCS_URL/tutorials/inputs/input_examples.html#keyboard-events`Input examples`/url` in the documentation for more information.
				**Note:** During input handling (e.g. `Node._input`), use `InputEvent.is_action_pressed` instead to query the action state of the current event. See also `is_action_just_pressed_by_event`.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert isinstance(exact_match, bool), 'exact_match must be bool'






    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)


    _ret = 0
    _ret = self._ptr.call_with_return(56,tuple([py_stringname_action._ptr, exact_match]))
    return _ret


  @functools.native_method
  def is_action_just_released(self, action:'object'   , exact_match:'bool' =False  ) -> typing.Union[bool]:
    r'''
				Returns `true` when the user _stops_ pressing the action event in the current frame or physics tick. It will only return `true` on the frame or tick that the user releases the button.
				**Note:** Returning `true` does not imply that the action is _still_ not pressed. An action can be released and pressed again rapidly, and `true` will still be returned so as not to miss input.
				If `exact_match` is `false`, it ignores additional input modifiers for `InputEventKey` and `InputEventMouseButton` events, and the direction for `InputEventJoypadMotion` events.
				**Note:** During input handling (e.g. `Node._input`), use `InputEvent.is_action_released` instead to query the action state of the current event. See also `is_action_just_released_by_event`.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert isinstance(exact_match, bool), 'exact_match must be bool'






    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)


    _ret = 0
    _ret = self._ptr.call_with_return(57,tuple([py_stringname_action._ptr, exact_match]))
    return _ret


  @functools.native_method
  def is_action_just_pressed_by_event(self, action:'object'   , event:'py4godot_inputevent.InputEvent'   , exact_match:'bool' =False  ) -> typing.Union[bool]:
    r'''
				Returns `true` when the user has _started_ pressing the action event in the current frame or physics tick, and the first event that triggered action press in the current frame/physics tick was `event`. It will only return `true` on the frame or tick that the user pressed down the button.
				This is useful for code that needs to run only once when an action is pressed, and the action is processed during input handling (e.g. `Node._input`).
				If `exact_match` is `false`, it ignores additional input modifiers for `InputEventKey` and `InputEventMouseButton` events, and the direction for `InputEventJoypadMotion` events.
				**Note:** Returning `true` does not imply that the action is _still_ pressed. An action can be pressed and released again rapidly, and `true` will still be returned so as not to miss input.
				**Note:** Due to keyboard ghosting, `is_action_just_pressed` may return `false` even if one of the action's keys is pressed. See `url=$DOCS_URL/tutorials/inputs/input_examples.html#keyboard-events`Input examples`/url` in the documentation for more information.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert(not event is None)
    assert isinstance(event, get_class('InputEvent')), 'event must be InputEvent'
    assert isinstance(exact_match, bool), 'exact_match must be bool'


    event.reference()




    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)



    _ret = 0
    _ret = self._ptr.call_with_return(58,tuple([py_stringname_action._ptr, event._ptr, exact_match]))
    return _ret


  @functools.native_method
  def is_action_just_released_by_event(self, action:'object'   , event:'py4godot_inputevent.InputEvent'   , exact_match:'bool' =False  ) -> typing.Union[bool]:
    r'''
				Returns `true` when the user _stops_ pressing the action event in the current frame or physics tick, and the first event that triggered action release in the current frame/physics tick was `event`. It will only return `true` on the frame or tick that the user releases the button.
				This is useful when an action is processed during input handling (e.g. `Node._input`).
				**Note:** Returning `true` does not imply that the action is _still_ not pressed. An action can be released and pressed again rapidly, and `true` will still be returned so as not to miss input.
				If `exact_match` is `false`, it ignores additional input modifiers for `InputEventKey` and `InputEventMouseButton` events, and the direction for `InputEventJoypadMotion` events.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert(not event is None)
    assert isinstance(event, get_class('InputEvent')), 'event must be InputEvent'
    assert isinstance(exact_match, bool), 'exact_match must be bool'


    event.reference()




    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)



    _ret = 0
    _ret = self._ptr.call_with_return(59,tuple([py_stringname_action._ptr, event._ptr, exact_match]))
    return _ret


  @functools.native_method
  def get_action_strength(self, action:'object'   , exact_match:'bool' =False  ) -> typing.Union[float]:
    r'''
				Returns a value between 0 and 1 representing the intensity of the given action. In a joypad, for example, the further away the axis (analog sticks or L2, R2 triggers) is from the dead zone, the closer the value will be to 1. If the action is mapped to a control that has no axis such as the keyboard, the value returned will be 0 or 1.
				If `exact_match` is `false`, it ignores additional input modifiers for `InputEventKey` and `InputEventMouseButton` events, and the direction for `InputEventJoypadMotion` events.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert isinstance(exact_match, bool), 'exact_match must be bool'






    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)


    _ret = 0
    _ret = self._ptr.call_with_return(60,tuple([py_stringname_action._ptr, exact_match]))
    return _ret


  @functools.native_method
  def get_action_raw_strength(self, action:'object'   , exact_match:'bool' =False  ) -> typing.Union[float]:
    r'''
				Returns a value between 0 and 1 representing the raw intensity of the given action, ignoring the action's deadzone. In most cases, you should use `get_action_strength` instead.
				If `exact_match` is `false`, it ignores additional input modifiers for `InputEventKey` and `InputEventMouseButton` events, and the direction for `InputEventJoypadMotion` events.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert isinstance(exact_match, bool), 'exact_match must be bool'






    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)


    _ret = 0
    _ret = self._ptr.call_with_return(61,tuple([py_stringname_action._ptr, exact_match]))
    return _ret


  @functools.native_method
  def get_axis(self, negative_action:'object'   , positive_action:'object'   ) -> typing.Union[float]:
    r'''
				Get axis input by specifying two actions, one negative and one positive.
				This is a shorthand for writing `Input.get_action_strength("positive_action") - Input.get_action_strength("negative_action")`.
			'''

    assert(not negative_action is None)
    assert isinstance(negative_action, (str, StringName)), 'negative_action must be str or StringName'
    assert(not positive_action is None)
    assert isinstance(positive_action, (str, StringName)), 'positive_action must be str or StringName'






    assert(isinstance(negative_action, (str, StringName)))
    py_stringname_negative_action = negative_action if isinstance(negative_action, StringName) else c_utils.py_string_to_string_name(negative_action)
    assert(isinstance(positive_action, (str, StringName)))
    py_stringname_positive_action = positive_action if isinstance(positive_action, StringName) else c_utils.py_string_to_string_name(positive_action)

    _ret = 0
    _ret = self._ptr.call_with_return(62,tuple([py_stringname_negative_action._ptr, py_stringname_positive_action._ptr]))
    return _ret


  @functools.native_method
  def get_vector(self, negative_x:'object'   , positive_x:'object'   , negative_y:'object'   , positive_y:'object'   , deadzone:'float' =-1.0  ) -> typing.Union['Vector2']:
    r'''
				Gets an input vector by specifying four actions for the positive and negative X and Y axes.
				This method is useful when getting vector input, such as from a joystick, directional pad, arrows, or WASD. The vector has its length limited to 1 and has a circular deadzone, which is useful for using vector input as movement.
				By default, the deadzone is automatically calculated from the average of the action deadzones. However, you can override the deadzone to be whatever you want (on the range of 0 to 1).
			'''

    assert(not negative_x is None)
    assert isinstance(negative_x, (str, StringName)), 'negative_x must be str or StringName'
    assert(not positive_x is None)
    assert isinstance(positive_x, (str, StringName)), 'positive_x must be str or StringName'
    assert(not negative_y is None)
    assert isinstance(negative_y, (str, StringName)), 'negative_y must be str or StringName'
    assert(not positive_y is None)
    assert isinstance(positive_y, (str, StringName)), 'positive_y must be str or StringName'
    assert isinstance(deadzone, (int, float)), 'deadzone must be int or float'






    assert(isinstance(negative_x, (str, StringName)))
    py_stringname_negative_x = negative_x if isinstance(negative_x, StringName) else c_utils.py_string_to_string_name(negative_x)
    assert(isinstance(positive_x, (str, StringName)))
    py_stringname_positive_x = positive_x if isinstance(positive_x, StringName) else c_utils.py_string_to_string_name(positive_x)
    assert(isinstance(negative_y, (str, StringName)))
    py_stringname_negative_y = negative_y if isinstance(negative_y, StringName) else c_utils.py_string_to_string_name(negative_y)
    assert(isinstance(positive_y, (str, StringName)))
    py_stringname_positive_y = positive_y if isinstance(positive_y, StringName) else c_utils.py_string_to_string_name(positive_y)


    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(63,tuple([py_stringname_negative_x._ptr, py_stringname_positive_x._ptr, py_stringname_negative_y._ptr, py_stringname_positive_y._ptr, deadzone]))
    return _ret


  @functools.native_method
  def add_joy_mapping(self, mapping:'str'   , update_existing:'bool' =False  ) -> None:
    r'''
				Adds a new mapping entry (in SDL2 format) to the mapping database. Optionally update already connected devices.
			'''

    assert(not mapping is None)
    assert isinstance(mapping, (str, String)), 'mapping must be str or String'
    assert isinstance(update_existing, bool), 'update_existing must be bool'






    assert(isinstance(mapping, (str, String)))
    py_string_mapping = mapping if isinstance(mapping, StringName) else c_utils.py_string_to_string(mapping)


    self._ptr.call_with_return(64,tuple([py_string_mapping._ptr, update_existing]))

  @functools.native_method
  def remove_joy_mapping(self, guid:'str'   ) -> None:
    r'''
				Removes all mappings from the internal database that match the given GUID. All currently connected joypads that use this GUID will become unmapped.
				On Android, Godot will map to an internal fallback mapping.
			'''

    assert(not guid is None)
    assert isinstance(guid, (str, String)), 'guid must be str or String'






    assert(isinstance(guid, (str, String)))
    py_string_guid = guid if isinstance(guid, StringName) else c_utils.py_string_to_string(guid)

    self._ptr.call_with_return(65,tuple([py_string_guid._ptr]))

  @functools.native_method
  def is_joy_known(self, device:'int'   ) -> typing.Union[bool]:
    r'''
				Returns `true` if the system knows the specified device. This means that it sets all button and axis indices. Unknown joypads are not expected to match these constants, but you can still retrieve events from them.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'








    _ret = 0
    _ret = self._ptr.call_with_return(66,tuple([device]))
    return _ret


  @functools.native_method
  def get_joy_axis(self, device:'int'   , axis:'int'  ) -> typing.Union[float]:
    r'''
				Returns the current value of the joypad axis at index `axis`.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'
    assert isinstance(axis, (int, float)), 'axis must be int or float'









    _ret = 0
    _ret = self._ptr.call_with_return(67,tuple([device, axis]))
    return _ret


  @functools.native_method
  def get_joy_name(self, device:'int'   ) -> typing.Union[str]:
    r'''
				Returns the name of the joypad at the specified device index, e.g. `PS4 Controller`. Godot uses the `url=https://github.com/gabomdq/SDL_GameControllerDB`SDL2 game controller database`/url` to determine gamepad names.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(68,tuple([device]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_joy_guid(self, device:'int'   ) -> typing.Union[str]:
    r'''
				Returns an SDL2-compatible device GUID on platforms that use gamepad remapping, e.g. `030000004c050000c405000000010000`. Returns an empty string if it cannot be found. Godot uses the `url=https://github.com/gabomdq/SDL_GameControllerDB`SDL2 game controller database`/url` to determine gamepad names and mappings based on this GUID.
				On Windows, all XInput joypad GUIDs will be overridden by Godot to `__XINPUT_DEVICE__`, because their mappings are the same.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'








    _ret = String.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(69,tuple([device]))
    return utils.gd_string_to_py_string(_ret)


  @functools.native_method
  def get_joy_info(self, device:'int'   ) -> typing.Union['Dictionary']:
    r'''
				Returns a dictionary with extra platform-specific information about the device, e.g. the raw gamepad name from the OS or the Steam Input index.
				On Windows, Linux, and macOS, the dictionary contains the following fields:
				`raw_name`: The name of the controller as it came from the OS, before getting renamed by the controller database.
				`vendor_id`: The USB vendor ID of the device.
				`product_id`: The USB product ID of the device.
				`steam_input_index`: The Steam Input gamepad index, if the device is not a Steam Input device this key won't be present.
				On Windows, the dictionary can have an additional field:
				`xinput_index`: The index of the controller in the XInput system. This key won't be present for devices not handled by XInput.
				**Note:** The returned dictionary is always empty on Android, iOS, visionOS, and Web.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'








    _ret = Dictionary.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(70,tuple([device]))
    return _ret


  @functools.native_method
  def should_ignore_device(self, vendor_id:'int'   , product_id:'int'   ) -> typing.Union[bool]:
    r'''
				Queries whether an input device should be ignored or not. Devices can be ignored by setting the environment variable `SDL_GAMECONTROLLER_IGNORE_DEVICES`. Read the `url=https://wiki.libsdl.org/SDL2`SDL documentation`/url` for more information.
				**Note:** Some 3rd party tools can contribute to the list of ignored devices. For example, _SteamInput_ creates virtual devices from physical devices for remapping purposes. To avoid handling the same input device twice, the original device is added to the ignore list.
			'''

    assert isinstance(vendor_id, (int, float)), 'vendor_id must be int or float'
    assert isinstance(product_id, (int, float)), 'product_id must be int or float'









    _ret = 0
    _ret = self._ptr.call_with_return(71,tuple([vendor_id, product_id]))
    return _ret


  @functools.native_method
  def get_connected_joypads(self) -> typing.Union['py4godot_inttypedarray.intTypedArray']:
    r'''
				Returns an `Array` containing the device IDs of all currently connected joypads.
			'''




    _ret = py4godot_inttypedarray.intTypedArray.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(72,tuple([]))
    return _ret


  @functools.native_method
  def get_joy_vibration_strength(self, device:'int'   ) -> typing.Union['Vector2']:
    r'''
				Returns the strength of the joypad vibration: x is the strength of the weak motor, and y is the strength of the strong motor.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'








    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(73,tuple([device]))
    return _ret


  @functools.native_method
  def get_joy_vibration_duration(self, device:'int'   ) -> typing.Union[float]:
    r'''
				Returns the duration of the current vibration effect in seconds.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'








    _ret = 0
    _ret = self._ptr.call_with_return(74,tuple([device]))
    return _ret


  @functools.native_method
  def start_joy_vibration(self, device:'int'   , weak_magnitude:'float'   , strong_magnitude:'float'   , duration:'float' =0  ) -> None:
    r'''
				Starts to vibrate the joypad. Joypads usually come with two rumble motors, a strong and a weak one. `weak_magnitude` is the strength of the weak motor (between 0 and 1) and `strong_magnitude` is the strength of the strong motor (between 0 and 1). `duration` is the duration of the effect in seconds (a duration of 0 will try to play the vibration indefinitely). The vibration can be stopped early by calling `stop_joy_vibration`.
				**Note:** Not every hardware is compatible with long effect durations; it is recommended to restart an effect if it has to be played for more than a few seconds.
				**Note:** For macOS, vibration is only supported in macOS 11 and later.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'
    assert isinstance(weak_magnitude, (int, float)), 'weak_magnitude must be int or float'
    assert isinstance(strong_magnitude, (int, float)), 'strong_magnitude must be int or float'
    assert isinstance(duration, (int, float)), 'duration must be int or float'











    self._ptr.call_with_return(75,tuple([device, weak_magnitude, strong_magnitude, duration]))

  @functools.native_method
  def stop_joy_vibration(self, device:'int'   ) -> None:
    r'''
				Stops the vibration of the joypad started with `start_joy_vibration`.
			'''

    assert isinstance(device, (int, float)), 'device must be int or float'








    self._ptr.call_with_return(76,tuple([device]))

  @functools.native_method
  def vibrate_handheld(self, duration_ms:'int' =500  , amplitude:'float' =-1.0  ) -> None:
    r'''
				Vibrate the handheld device for the specified duration in milliseconds.
				`amplitude` is the strength of the vibration, as a value between `0.0` and `1.0`. If set to `-1.0`, the default vibration strength of the device is used.
				**Note:** This method is implemented on Android, iOS, and Web. It has no effect on other platforms.
				**Note:** For Android, `vibrate_handheld` requires enabling the `VIBRATE` permission in the export preset. Otherwise, `vibrate_handheld` will have no effect.
				**Note:** For iOS, specifying the duration is only supported in iOS 13 and later.
				**Note:** For Web, the amplitude cannot be changed.
				**Note:** Some web browsers such as Safari and Firefox for Android do not support `vibrate_handheld`.
			'''

    assert isinstance(duration_ms, (int, float)), 'duration_ms must be int or float'
    assert isinstance(amplitude, (int, float)), 'amplitude must be int or float'









    self._ptr.call_with_return(77,tuple([duration_ms, amplitude]))

  @functools.native_method
  def get_gravity(self) -> typing.Union['Vector3']:
    r'''
				Returns the gravity in m/s² of the device's accelerometer sensor, if the device has one. Otherwise, the method returns `constant Vector3.ZERO`.
				**Note:** This method only works on Android and iOS. On other platforms, it always returns `constant Vector3.ZERO`.
				**Note:** For Android, `ProjectSettings.input_devices/sensors/enable_gravity` must be enabled.
			'''




    _ret = Vector3.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(78,tuple([]))
    return _ret


  @functools.native_method
  def get_accelerometer(self) -> typing.Union['Vector3']:
    r'''
				Returns the acceleration in m/s² of the device's accelerometer sensor, if the device has one. Otherwise, the method returns `constant Vector3.ZERO`.
				Note this method returns an empty `Vector3` when running from the editor even when your device has an accelerometer. You must export your project to a supported device to read values from the accelerometer.
				**Note:** This method only works on Android and iOS. On other platforms, it always returns `constant Vector3.ZERO`.
				**Note:** For Android, `ProjectSettings.input_devices/sensors/enable_accelerometer` must be enabled.
			'''




    _ret = Vector3.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(79,tuple([]))
    return _ret


  @functools.native_method
  def get_magnetometer(self) -> typing.Union['Vector3']:
    r'''
				Returns the magnetic field strength in micro-Tesla for all axes of the device's magnetometer sensor, if the device has one. Otherwise, the method returns `constant Vector3.ZERO`.
				**Note:** This method only works on Android and iOS. On other platforms, it always returns `constant Vector3.ZERO`.
				**Note:** For Android, `ProjectSettings.input_devices/sensors/enable_magnetometer` must be enabled.
			'''




    _ret = Vector3.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(80,tuple([]))
    return _ret


  @functools.native_method
  def get_gyroscope(self) -> typing.Union['Vector3']:
    r'''
				Returns the rotation rate in rad/s around a device's X, Y, and Z axes of the gyroscope sensor, if the device has one. Otherwise, the method returns `constant Vector3.ZERO`.
				**Note:** This method only works on Android and iOS. On other platforms, it always returns `constant Vector3.ZERO`.
				**Note:** For Android, `ProjectSettings.input_devices/sensors/enable_gyroscope` must be enabled.
			'''




    _ret = Vector3.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(81,tuple([]))
    return _ret


  @functools.native_method
  def set_gravity(self, value:'Vector3'   ) -> None:
    r'''
				Sets the gravity value of the accelerometer sensor. Can be used for debugging on devices without a hardware sensor, for example in an editor on a PC.
				**Note:** This value can be immediately overwritten by the hardware sensor value on Android and iOS.
			'''

    assert(not value is None)
    assert isinstance(value, Vector3), 'value must be Vector3'








    self._ptr.call_with_return(82,tuple([value._ptr]))

  @functools.native_method
  def set_accelerometer(self, value:'Vector3'   ) -> None:
    r'''
				Sets the acceleration value of the accelerometer sensor. Can be used for debugging on devices without a hardware sensor, for example in an editor on a PC.
				**Note:** This value can be immediately overwritten by the hardware sensor value on Android and iOS.
			'''

    assert(not value is None)
    assert isinstance(value, Vector3), 'value must be Vector3'








    self._ptr.call_with_return(83,tuple([value._ptr]))

  @functools.native_method
  def set_magnetometer(self, value:'Vector3'   ) -> None:
    r'''
				Sets the value of the magnetic field of the magnetometer sensor. Can be used for debugging on devices without a hardware sensor, for example in an editor on a PC.
				**Note:** This value can be immediately overwritten by the hardware sensor value on Android and iOS.
			'''

    assert(not value is None)
    assert isinstance(value, Vector3), 'value must be Vector3'








    self._ptr.call_with_return(84,tuple([value._ptr]))

  @functools.native_method
  def set_gyroscope(self, value:'Vector3'   ) -> None:
    r'''
				Sets the value of the rotation rate of the gyroscope sensor. Can be used for debugging on devices without a hardware sensor, for example in an editor on a PC.
				**Note:** This value can be immediately overwritten by the hardware sensor value on Android and iOS.
			'''

    assert(not value is None)
    assert isinstance(value, Vector3), 'value must be Vector3'








    self._ptr.call_with_return(85,tuple([value._ptr]))

  @functools.native_method
  def get_last_mouse_velocity(self) -> typing.Union['Vector2']:
    r'''
				Returns the last mouse velocity. To provide a precise and jitter-free velocity, mouse velocity is only calculated every 0.1s. Therefore, mouse velocity will lag mouse movements.
			'''




    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(86,tuple([]))
    return _ret


  @functools.native_method
  def get_last_mouse_screen_velocity(self) -> typing.Union['Vector2']:
    r'''
				Returns the last mouse velocity in screen coordinates. To provide a precise and jitter-free velocity, mouse velocity is only calculated every 0.1s. Therefore, mouse velocity will lag mouse movements.
			'''




    _ret = Vector2.construct_without_init()
    _ret._ptr = self._ptr.call_with_return(87,tuple([]))
    return _ret


  @functools.native_method
  def get_mouse_button_mask(self) -> typing.Union[int]:
    r'''
				Returns mouse buttons as a bitmask. If multiple mouse buttons are pressed at the same time, the bits are added together. Equivalent to `DisplayServer.mouse_get_button_state`.
			'''




    _ret
    _ret = self._ptr.call_with_return(88,tuple([]))
    return _ret


  @functools.native_method
  def set_mouse_mode(self, mode:'int'  ) -> None:
    r''''''

    assert isinstance(mode, (int, float)), 'mode must be int or float'



    self.py__mouse_mode = mode






    self._ptr.call_with_return(89,tuple([mode]))

  @functools.native_method
  def get_mouse_mode(self) -> typing.Union[int]:
    r''''''




    _ret:int
    _ret = self._ptr.call_with_return(90,tuple([]))

    return _ret


  @functools.native_method
  def warp_mouse(self, position:'Vector2'   ) -> None:
    r'''
				Sets the mouse position to the specified vector, provided in pixels and relative to an origin at the upper left corner of the currently focused Window Manager game window.
				Mouse position is clipped to the limits of the screen resolution, or to the limits of the game window if `enum MouseMode` is set to `constant MOUSE_MODE_CONFINED` or `constant MOUSE_MODE_CONFINED_HIDDEN`.
				**Note:** `warp_mouse` is only supported on Windows, macOS and Linux. It has no effect on Android, iOS and Web.
			'''

    assert(not position is None)
    assert isinstance(position, Vector2), 'position must be Vector2'








    self._ptr.call_with_return(91,tuple([position._ptr]))

  @functools.native_method
  def action_press(self, action:'object'   , strength:'float' =1.0  ) -> None:
    r'''
				This will simulate pressing the specified action.
				The strength can be used for non-boolean actions, it's ranged between 0 and 1 representing the intensity of the given action.
				**Note:** This method will not cause any `Node._input` calls. It is intended to be used with `is_action_pressed` and `is_action_just_pressed`. If you want to simulate `_input`, use `parse_input_event` instead.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'
    assert isinstance(strength, (int, float)), 'strength must be int or float'






    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)


    self._ptr.call_with_return(92,tuple([py_stringname_action._ptr, strength]))

  @functools.native_method
  def action_release(self, action:'object'   ) -> None:
    r'''
				If the specified action is already pressed, this will release it.
			'''

    assert(not action is None)
    assert isinstance(action, (str, StringName)), 'action must be str or StringName'






    assert(isinstance(action, (str, StringName)))
    py_stringname_action = action if isinstance(action, StringName) else c_utils.py_string_to_string_name(action)

    self._ptr.call_with_return(93,tuple([py_stringname_action._ptr]))

  @functools.native_method
  def set_default_cursor_shape(self, shape:'int'  =0) -> None:
    r'''
				Sets the default cursor shape to be used in the viewport instead of `constant CURSOR_ARROW`.
				**Note:** If you want to change the default cursor shape for `Control`'s nodes, use `Control.mouse_default_cursor_shape` instead.
				**Note:** This method generates an `InputEventMouseMotion` to update cursor immediately.
			'''


    assert isinstance(shape, (int, float)), 'shape must be int or float'








    self._ptr.call_with_return(94,tuple([shape]))

  @functools.native_method
  def get_current_cursor_shape(self) -> typing.Union[int]:
    r'''
				Returns the currently assigned cursor shape.
			'''




    _ret:int
    _ret = self._ptr.call_with_return(95,tuple([]))
    return _ret


  @functools.native_method
  def set_custom_mouse_cursor(self, image:'py4godot_resource.Resource'   , shape:'int'  =0, hotspot:'Vector2' = None  ) -> None:
    r'''
				Sets a custom mouse cursor image, which is only visible inside the game window, for the given mouse `shape`. The hotspot can also be specified. Passing `null` to the image parameter resets to the system cursor.
				`image` can be either `Texture2D` or `Image` and its size must be lower than or equal to 256×256. To avoid rendering issues, sizes lower than or equal to 128×128 are recommended.
				`hotspot` must be within `image`'s size.
				**Note:** `AnimatedTexture`s aren't supported as custom mouse cursors. If using an `AnimatedTexture`, only the first frame will be displayed.
				**Note:** The **Lossless**, **Lossy** or **Uncompressed** compression modes are recommended. The **Video RAM** compression mode can be used, but it will be decompressed on the CPU, which means loading times are slowed down and no memory is saved compared to lossless modes.
				**Note:** On the web platform, the maximum allowed cursor image size is 128×128. Cursor images larger than 32×32 will also only be displayed if the mouse cursor image is entirely located within the page for `url=https://chromestatus.com/feature/5825971391299584`security reasons`/url`.
			'''

    if hotspot is None:
      hotspot = Vector2.new0()

    assert(not image is None)
    assert isinstance(image, get_class('Resource')), 'image must be Resource'
    assert isinstance(shape, (int, float)), 'shape must be int or float'


    image.reference()








    self._ptr.call_with_return(96,tuple([image._ptr, shape, hotspot._ptr]))

  @functools.native_method
  def parse_input_event(self, event:'py4godot_inputevent.InputEvent'   ) -> None:
    r'''
				Feeds an `InputEvent` to the game. Can be used to artificially trigger input events from code. Also generates `Node._input` calls.
				
				```gdscript
				var cancel_event = InputEventAction.new()
				cancel_event.action = "ui_cancel"
				cancel_event.pressed = true
				Input.parse_input_event(cancel_event)
				```
				
				
				**Note:** Calling this function has no influence on the operating system. So for example sending an `InputEventMouseMotion` will not move the OS mouse cursor to the specified position (use `warp_mouse` instead) and sending `kbd`Alt/Cmd + Tab`/kbd` as `InputEventKey` won't toggle between active windows.
			'''

    assert(not event is None)
    assert isinstance(event, get_class('InputEvent')), 'event must be InputEvent'


    event.reference()






    self._ptr.call_with_return(97,tuple([event._ptr]))

  @functools.native_method
  def set_use_accumulated_input(self, enable:'bool'   ) -> None:
    r''''''

    assert isinstance(enable, bool), 'enable must be bool'



    self.py__use_accumulated_input = enable






    self._ptr.call_with_return(98,tuple([enable]))

  @functools.native_method
  def is_using_accumulated_input(self) -> typing.Union[bool]:
    r''''''




    _ret = 0
    _ret = self._ptr.call_with_return(99,tuple([]))

    return _ret


  @functools.native_method
  def flush_buffered_events(self) -> None:
    r'''
				Sends all input events which are in the current buffer to the game loop. These events may have been buffered as a result of accumulated input (`use_accumulated_input`) or agile input flushing (`ProjectSettings.input_devices/buffering/agile_event_flushing`).
				The engine will already do this itself at key execution points (at least once per frame). However, this can be useful in advanced cases where you want precise control over the timing of event handling.
			'''




    self._ptr.call_with_return(100,tuple([]))

  @functools.native_method
  def set_emulate_mouse_from_touch(self, enable:'bool'   ) -> None:
    r''''''

    assert isinstance(enable, bool), 'enable must be bool'



    self.py__emulate_mouse_from_touch = enable






    self._ptr.call_with_return(101,tuple([enable]))

  @functools.native_method
  def is_emulating_mouse_from_touch(self) -> typing.Union[bool]:
    r''''''




    _ret = 0
    _ret = self._ptr.call_with_return(102,tuple([]))

    return _ret


  @functools.native_method
  def set_emulate_touch_from_mouse(self, enable:'bool'   ) -> None:
    r''''''

    assert isinstance(enable, bool), 'enable must be bool'



    self.py__emulate_touch_from_mouse = enable






    self._ptr.call_with_return(103,tuple([enable]))

  @functools.native_method
  def is_emulating_touch_from_mouse(self) -> typing.Union[bool]:
    r''''''




    _ret = 0
    _ret = self._ptr.call_with_return(104,tuple([]))

    return _ret



register_cast_function('Input', Input.cast)
register_class('Input', Input)
